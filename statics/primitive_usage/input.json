{
  "classification": "delegated",
  "primitiveModules": [
    "icons"
  ],
  "reason": "When `type` is \"password\", the field owns a small reveal-toggle state (`use_signal`) and renders the shared `Eye`/`EyeOff` icons; every other `type` still renders a bare styled `<input>` with no controlled state."
}
