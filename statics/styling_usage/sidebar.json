{
  "tailwindOnly": false,
  "styleException": [
    {
      "description": "--sidebar-width/--sidebar-width-icon CSS custom properties (sidebar.rs:111)",
      "reason": "Tailwind utility classes cannot set CSS custom properties; these two custom properties feed the sidebar's own Tailwind width classes elsewhere in the file."
    }
  ],
  "tokenCompliant": true,
  "colorException": [],
  "radiusException": [
    {
      "part": "root",
      "reason": "Sidebar's Floating-variant radius is switched by the variant itself, not a free-standing cosmetic dial"
    },
    {
      "part": "inset",
      "reason": "SidebarInset's Inset-variant radius is switched by the variant itself, not a free-standing cosmetic dial"
    },
    {
      "part": "group-label",
      "reason": "SidebarGroupLabel is a small internal chrome detail, not an independently-tunable surface"
    }
  ],
  "inspiredBy": [
    "shadcn",
    "dioxus-components"
  ],
  "inspirationNote": "bg-sidebar/bg-sidebar-accent/text-sidebar-accent-foreground token family matches shadcn's own sidebar token names; custom-property styleException already recorded, and shadcn's own sidebar also relies on a --sidebar-width custom property for the same reason. The open-panel width classes were corrected from w-[--sidebar-width]/w-[--sidebar-width-icon] to Tailwind v4's own var-reference syntax w-(--sidebar-width)/w-(--sidebar-width-icon) -- the bracket form compiled to the invalid declaration `width: --sidebar-width` (silently dropped, panel rendered at shrink-to-fit width instead of the intended 16rem) -- plus a defensive max-w-[85vw] clamp on the open panel; mobile Sheet-mode remains deferred per this file's own module doc, JS viewport detection being non-functional in this runtime (make-registry-components-mobile-first). dioxus-components uses CSS modules (external style.css files), not Tailwind, so no Tailwind class-level comparison is possible there."
}
