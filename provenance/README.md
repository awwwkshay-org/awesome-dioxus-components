# Provenance records

`schema.json` defines the record required for source imported into adico.
`records/example-dioxus-components.json` is a schema fixture only: its all-zero
revision and example paths prove format shape and must never be used for an
actual import.

M1 replaces the example with records that name real immutable revisions before
adding upstream-derived source files.
