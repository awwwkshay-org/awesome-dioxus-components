[workspace]
members = [
    "apps/docs",
    "apps/playground",
    "examples/basic",
    "examples/dashboard",
    "examples/desktop",
    "examples/forms",
    "examples/fullstack",
    "examples/kitchen-sink",
    "examples/web",
    "packages/adico-cli",
    "packages/adico-primitives",
    "packages/adico-registry-core",
    "packages/adico-test-utils",
    "packages/adico-xtask",
    "packages/shared",
]
resolver = "3"

[workspace.package]
version = "0.1.0"
edition = "2024"
license = "MIT OR Apache-2.0"

[workspace.dependencies]
axum = { version = "0.8", features = ["macros"] }
dioxus = { version = "=0.7.9", features = ["fullstack", "router"] }
reqwest = { version = "0.12", default-features = false, features = ["json"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sqlx = { version = "0.8", default-features = false, features = ["runtime-tokio-rustls", "postgres", "uuid", "migrate", "macros"] }
thiserror = "2"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "net", "signal"] }
tower-http = { version = "0.6", features = ["trace"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
uuid = { version = "1", features = ["serde", "v4", "js"] }
