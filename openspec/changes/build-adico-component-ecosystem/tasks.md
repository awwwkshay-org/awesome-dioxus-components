## 1. M0 — Repository and architecture foundation

- [x] 1.1 Audit the current Cargo workspace, current application/template assets, and OpenSpec baseline; record the disposition of every pre-adico package and verify the audit is reviewed in the M0 decision record.
- [x] 1.2 Define the target Cargo workspace members and create the `apps`, `packages`, `examples`, `registry`, `tests`, and `scripts` boundaries without moving implementation prematurely; verify `cargo metadata --locked` reports the intended skeleton.
- [x] 1.3 Create package skeletons for `adico-cli`, `adico-primitives`, `adico-registry-core`, `adico-test-utils`, and `adico-xtask`; verify each has a deliberate public boundary and the workspace builds with `cargo check --workspace --locked`.
- [x] 1.4 Create docs and playground application skeletons plus the basic, web, desktop, fullstack, forms, dashboard, and kitchen-sink example manifests; verify workspace members do not import registry UI source as a crate.
- [x] 1.5 Establish the test directory layout, CI baseline, and command matrix for formatting, lint, unit, compile, installation, browser, SSR/hydration, desktop, and visual checks; verify CI/documentation distinguish required from optional runners.
- [x] 1.6 Investigate and pin the Dioxus 0.7-compatible Tailwind workflow, CSS entry conventions, Lucide icon crate, browser test runner, and visual test tool; verify the resulting compatibility decision names tested versions, licenses, and platform support.
- [x] 1.7 Add repository dual-license files, contribution/provenance policy, and `UPSTREAMS.md`/record schema; verify a sample provenance record passes schema validation.
- [x] 1.8 Define the parity manifest JSON schema, source-of-truth ownership, evidence vocabulary, and offline validation rules; verify a minimal fixture is accepted and an incomplete-complete fixture is rejected.
- [x] 1.9 Record M0 acceptance: workspace/package boundaries, toolchain decisions, licensing policy, test baseline, and parity schema are approved; verify the decision record links all artifacts and `cargo fmt --all --check` passes.

## 2. M1 — Upstream inventory and primitive ownership

- [x] 2.1 Fetch and pin the current `DioxusLabs/dioxus-components` revision; inventory every styled component, primitive, test, CSS/theme asset, dependency edge, and license source, and verify the inventory is reproducible from the pinned revision.
- [x] 2.2 Classify every upstream styled component as `EXISTING_SHADCN_EQUIVALENT`, `EXISTING_DIOXUS_EXTRA`, `NEEDS_PARITY_UPDATES`, `NEEDS_PRIMITIVE_FIX`, or `NOT_SUITABLE_FOR_REUSE`; verify all inventory entries have one classification and rationale.
- [x] 2.3 Map upstream styled components to the checked-in current shadcn catalog snapshot and record existing-shadcn matches, Dioxus-only extras, and tentative gap groups; verify the mapping has no unnamed upstream item.
- [x] 2.4 Record the primitive dependency graph for all reusable candidates and select the minimal Button/Dialog/third-component runtime slice; verify the chosen third component is Select, Combobox, or Calendar based on actual source suitability and interaction coverage.
- [x] 2.5 Import/fork the selected primitive modules into `adico-primitives` with immutable provenance, retained notices, and public-facade boundaries; verify `adico-xtask provenance check` succeeds for every imported file.
- [x] 2.6 Preserve required upstream internal module structure for the initial primitive slice and add independent unit/compile coverage before refactoring; verify `cargo test -p adico-primitives --locked` passes.
- [x] 2.7 Add target-gated runtime adapters for SSR-safe behavior and the selected web/desktop capabilities; verify the primitive crate compiles for native, server, and `wasm32-unknown-unknown` targets required by the selected slice.
- [x] 2.8 Port or adapt reusable upstream test-harness helpers into `adico-test-utils`; verify a primitive-focused keyboard/focus test runs through the common helper.
- [x] 2.9 Add `adico-xtask upstream dioxus-components` inventory refresh and diff reporting; verify it produces no live-network requirement for normal CI after its snapshot is checked in.
- [x] 2.10 Record M1 acceptance: pinned inventory, classifications, provenance, selected owned primitives, and independent target checks are complete; verify all M1 reports are linked from `UPSTREAMS.md`.

## 3. M2 — Registry and CLI vertical slice

- [x] 3.1 Define Rust types and JSON schema for source registry manifests, named embedded/local/static-HTTPS registry sources, namespaces, default-registry selection, item metadata, source checksums, file target roots, registry dependencies, Cargo dependencies, module exports, style requirements, compatibility, and provenance; verify valid and invalid official and Awwwkshay fixture manifests deserialize as expected.
- [x] 3.2 Choose and implement the canonical authored item layout plus generated normalized local index/payloads; verify `cargo xtask registry build` is deterministic across two clean runs.
- [x] 3.3 Implement registry source loading for the embedded official registry, a configured local path, and static HTTPS, then validate source paths, item names/types, compatibility ranges, duplicate target intent, dependency references, cycles, checksums, and HTTPS-only network endpoints; verify negative fixtures fail with actionable diagnostics before project mutation.
- [x] 3.4 Implement deterministic transitive registry dependency resolution and install-plan generation in `adico-registry-core`, preserving source namespace for every item and requiring explicit cross-registry dependencies; verify unit tests cover shared dependencies, missing items, cycles, stable ordering, default-registry lookup, and cross-registry resolution.
- [x] 3.5 Implement Cargo dependency requirement unification, including package aliases, features, default-feature policy, and target predicates; verify compatible requirements merge and incompatible requirements identify their origins.
- [x] 3.6 Define and implement versioned `components.json` parsing/validation and migration rules for named registries and `defaultRegistry`; verify invalid paths/URLs/namespaces, unsupported versions, and valid official and company defaults are covered by tests.
- [x] 3.7 Implement Dioxus project discovery using nearest Cargo.toml, Cargo metadata, Dioxus dependency inspection, entrypoint checks, and explicit ambiguity failures; verify single-package, workspace, non-Dioxus, and ambiguous fixtures.
- [x] 3.8 Implement `adico init` directory/config/module/CSS preparation and explicit default-registry selection with a reviewable plan; verify fresh official and Awwwkshay-registry fixtures obtain valid `components.json` and no unrelated file changes.
- [x] 3.9 Implement marker-region `mod.rs` creation and updater with deterministic declarations/re-exports, idempotency, and malformed/duplicate marker rejection; verify preservation fixtures retain all bytes outside the region.
- [x] 3.10 Implement structured Cargo.toml edits with `toml_edit` for ordinary package and unambiguous workspace dependencies; verify comments remain preserved where TOML permits and conflicts make no manifest edit.
- [x] 3.11 Implement idempotent CSS/theme token installation using the selected Tailwind/Dioxus workflow and explicit marker ownership; verify light/dark tokens and radius tokens are installed once without overwriting unrelated CSS.
- [x] 3.12 Implement `adico add <component...>` plan validation and transactional file apply with checksum-based unchanged/user-modified detection, source-namespace reporting, and `adico.lock` manifest/item pinning; verify planning failure makes no project changes and a modified file is reported without overwrite.
- [x] 3.13 Implement `adico add --all` using the same resolver/apply path and deterministic output; verify it installs every v1-supported local registry item once in a fixture.
- [x] 3.14 Add Button registry source/metadata, minimal source-installed class utility, theme requirements, and icon dependency declarations; verify `adico init && adico add button` yields a compiling consumer fixture.
- [x] 3.14a Implement read-only `adico list` and `adico view <item>` using configured registry resolution; verify default/explicit namespace selection, complete metadata output, and no consumer-project mutation.
- [x] 3.15 Add Dialog registry source/metadata and its owned primitive/Cargo/theme dependencies; verify an installed Dialog compiles and has browser tests for open, Escape, focus, ARIA association, outside interaction, nesting, and scroll locking as applicable.
- [x] 3.16 Add the M1-selected richer component registry source/metadata and dependencies; verify its installed fixture passes its critical keyboard/accessibility interaction suite.
- [x] 3.17 Create the basic consumer-style fixture by invoking the locally built `adico` binary for init and vertical-slice add, without workspace-path source imports; verify Cargo build and Dioxus web build succeed.
- [x] 3.18 Add CLI integration tests for multi-item add, repeated add, shared dependencies, Cargo conflicts, file conflicts, malformed modules, dry-plan output, incompatible registry sources, and source-lock refresh behavior; verify the installation test suite passes.
- [x] 3.19 Create an Awwwkshay curated-registry fixture with a local and static-HTTPS manifest, select it as `defaultRegistry`, and install bare plus explicit `@adico` items; verify dependency namespaces, generated source provenance, and consumer build outcomes are correct.
- [x] 3.20 Add vertical-slice SSR/hydration and desktop smoke validation with recorded target outcomes; verify `parity.json` contains evidence/skip reasons for each selected component.
- [x] 3.21 Record M2 acceptance: registry source→metadata→resolution→CLI→Cargo/module/CSS→consumer build/runtime pipeline passes for Button, Dialog, and the selected richer component from the official registry and a configured organization registry; verify the end-to-end acceptance command is documented and reproducible.

## 4. M3 — Existing component migration

- [x] 4.1 Turn the M1 inventory into a migration queue ordered by reusable primitive availability and classify each entry’s registry item type, target source files, theme assets, Cargo dependencies, and platform limitations; verify every suitable upstream item has a migration decision.
- [x] 4.2 Extend `adico-primitives` only with the audited upstream dependencies necessary for the first migration batch, preserving provenance and public facades; verify focused primitive tests and required target checks pass.
- [x] 4.3 Migrate the first independent batch of existing styled components into `registry/` with source metadata, docs metadata, and installation fixtures; verify each can be installed by `adico add` into a clean consumer fixture.
- [x] 4.4 Migrate overlay/layer-dependent existing components as a batch after their shared primitive behavior is available; verify browser keyboard, focus, and accessibility coverage for every migrated interactive item.
- [x] 4.5 Migrate collection/selection/navigation-dependent existing components as a batch after their shared primitive behavior is available; verify representative keyboard/typeahead/selection tests and consumer builds.
- [x] 4.5a Migrated the M3 migration queue's Wave 2 batch (accordion, alert-dialog, aspect-ratio, avatar, checkbox, collapsible, label, progress, radio-group, scroll-area, slider, switch, tabs, toast, toggle, toggle-group) — a real gap discovered during 4.5: the migration queue defines five waves, but this file's M3 section only ever had checkboxes for waves 1/3/4/5, not 2. `separator` (also Wave 2) was already migrated early as a hard dependency of `sidebar`, per `docs/adico/m3-wave4-migration.md`, and stays out of this task's explicit sixteen-item scope. Worked across four sub-batches by primitive-port cost, each independently registry-validated, provenance-tracked, compile/feature-checked (native, `--features web --target wasm32-unknown-unknown`, `--features desktop`), installed through the real `adico` binary into clean consumer fixtures, and — for every item with real keyboard/pointer/focus behavior — verified live via `dx serve` + Playwright + axe with zero critical violations (`tests/playwright/wave2-state.spec.ts`, `wave2-roving-focus.spec.ts`, `wave2-risk.spec.ts`; non-interactive items follow the Wave-1-established precedent of no dedicated spec). `toggle-group` was deliberately deferred within its sub-batch until its hard dependency (`crate::toggle::Toggle`) landed from a concurrently-running fork, and `accordion`/`radio-group`/`tabs`/`toggle-group` all confirmed (by reading actual upstream source, not the queue's one-line descriptions) that they reuse the collection roving-focus infrastructure already imported for Select — no new shared primitive was needed there. Real defects were found and fixed purely by testing in a live browser, not by compiling: Popover-class Escape breakage (`use_global_escape_listener`'s long-lived `document::eval` listener never registers in this Dioxus 0.7.9/0.7.10 web runtime) recurred for `alert-dialog`, requiring `AlertDialogRoot`/`AlertDialogContent` to be restructured to match `dialog::DialogRoot`'s actual always-mounted-root/self-gating-content pattern plus a new `AlertDialogTrigger` nested inside (not upstream's external-sibling composition); and `AlertDialogOverlay` was found — via a real Playwright run, not the flaky browser-extension clicks used for interactive debugging — to block every click site-wide while closed because it never gated on open state. Full details, scope-decision rationale, and per-sub-batch verification tables are in `docs/adico/m3-wave2-migration.md`.
- [x] 4.6 Migrated the M3 migration queue's Wave 5 batch (`color-picker`, `drag-and-drop-list`, `tag-group`, `toolbar`, `virtual-list`), all classified `EXISTING_DIOXUS_EXTRA` in `upstreams/dioxus-components/inventory.md`. `form` is explicitly excluded: its upstream `component.rs` is a two-line re-export with an empty `docs.md`, and its only real content is a demo of a native `<form>` element plus the already-migrated Checkbox primitive — there is no Form component/primitive to fork, so migrating it would mean inventing behavior upstream never shipped. The false-parity-credit verification requirement is satisfied by the mechanism already documented in `docs/adico/parity.md` (`parity.json` only tracks items explicitly mapped to the upstream shadcn catalog, so none of these five get an entry) plus registry `description`/`compositionNote` wording that deliberately avoids shadcn-equivalence framing, rather than a new schema field. Worked across five sub-batches, each independently registry-validated, provenance-tracked (`provenance/records/adico-primitives-wave5-extras.json`), compile/feature-checked (native, `--features web --target wasm32-unknown-unknown`, `--features desktop`), installed through the real `adico` binary into clean consumer fixtures, and verified live via `dx serve` + Playwright + axe with zero critical violations (`tests/playwright/wave5-extras.spec.ts`, `wave5-tag-group.spec.ts`, `wave5-drag-and-drop-list.spec.ts`, `wave5-color-picker.spec.ts`). Two real defects were found and fixed purely by testing in a live browser, not by compiling: `virtual-list` always rendered zero items because upstream's long-lived `document::eval` scroll-subscription listener never registers in this Dioxus 0.7.9/0.7.10 web runtime (the same defect class already recorded for `use_global_escape_listener` in Wave 3) — fixed by replacing it entirely with native `onscroll`/`onmounted` events, which needed no `web`/`desktop` feature gate at all; and `color-picker`'s thumb intentionally suppresses click-driven focus (to avoid stealing focus during a pointer drag), which silently broke a click-based keyboard test until it was reached via `locator.focus()` instead. The migration queue's own one-line dependency notes were also found wrong for two items when checked against actual upstream source (per the M1 lesson): `color-picker` does not compose `label`/`popover`/`slider` as the queue stated, only the already-owned `move_interaction`/`pointer` modules. Two named, carried-forward gaps remain open rather than hidden: `drag-and-drop-list`'s native pointer/mouse drag path and `color-picker`'s vertical/value-input keyboard branch are compiled and wired but not independently browser-verified, matching the standing precedent of Slider's unverified desktop pointer-capture behavior. Full details, scope-decision rationale, and per-sub-batch verification tables are in `docs/adico/m3-wave5-migration.md`.
- [x] 4.7 Implemented `cargo xtask parity` (new, previously nonexistent) in `packages/adico-xtask/src/main.rs`: it loads `registry/registry.json`, excludes `registry:lib` items and a hardcoded 5-item `DIOXUS_ONLY_EXTRAS` constant (color-picker, drag-and-drop-list, tag-group, toolbar, virtual-list — commented as sourced from `upstreams/dioxus-components/inventory.md`'s `EXISTING_DIOXUS_EXTRA` classification, deliberately not read from `upstreams/dioxus-components/catalog.json`, which carries no classification field and is regenerable/clobberable), and reports any remaining registry item lacking a `parity.json` entry as unclassified. It also enforces the business rules `parity.schema.json` expresses as `if`/`then` conditionals (an inapplicable dimension must be unpassed with a note; a passed dimension needs evidence; a `complete` component needs every dimension applicable+passed) — hand-rolled in Rust rather than via a JSON Schema crate, matching the existing `registry validate`/`provenance check` style. Added `tests/compile/parity/` as a negative-fixture pair and reused the previously-unexercised `tests/compile/parity-valid.json`/`parity-invalid-complete.json` schema fixtures as regression tests (4 new unit tests, all passing). Along the way, found and fixed a real, inherited bug: `registry/generated/items/` only had 22 of 43 payloads (`cargo xtask registry build` had never been re-run since Wave 1), fixed in a preceding commit before this task's own work. Then added `parity.json` entries for all 34 `EXISTING_SHADCN_EQUIVALENT` registry items migrated in Waves 1–4 that didn't already have one (button/dialog/select already did), bringing `cargo xtask parity` to "37/37 registry items classified, 0 unclassified, 5 extras excluded." Every entry's evidence was gathered from actual repository state — the specific Playwright spec test title that exercises each item (`wave2-state.spec.ts`, `wave2-roving-focus.spec.ts`, `wave2-risk.spec.ts`, `wave3.spec.ts`, `wave4.spec.ts`), the `tests/installation/*-consumer` fixture that installed it, and the relevant `docs/adico/m3-wave*-migration.md` report — never fabricated. Reading `registry/ui/*.rs` directly (not the migration queue's one-line descriptions, per the standing M1 lesson) surfaced two real, previously unrecorded gaps: Item's `Interactive` variant sets `tabindex=0`/`onclick` but has no `onkeydown` handler, so it cannot be activated via Enter/Space by keyboard despite being focusable; and Sheet composes the owned Dialog primitive's Escape/focus-trap behavior but has no dedicated Playwright spec of its own, unlike Dialog. Both are recorded as named `applicable: true, passed: false` gaps, not silently marked passed or reclassified as inapplicable. `keyboard` was marked `applicable: false` only for genuinely non-focusable elements confirmed by reading source (Badge, Card, Skeleton, Aspect Ratio, Label, Progress, Avatar) — never as a substitute for "unverified." Every dimension not actually verified (api/visual/variants/states/darkMode/rtl/responsive/docs, `examples` beyond the original button/dialog/select vertical slice, `ssrHydration`, `desktop`) stays `applicable: true, passed: false` with a note naming the task that will close it (M4 tasks 5.2/5.3, M4/M10 tasks 5.5/11.5, or task 4.8 for examples/SSR breadth), matching the existing button/dialog/select template exactly — none of the 34 are marked `complete`, since M4 hardening genuinely hasn't happened yet. Documentation metadata was confirmed already complete for all 43 registry items (none missing a `documentation` block) — no action needed there. Explicit scope decision, recorded here rather than duplicated: this task records accurate parity status for coverage that *already exists* from the Wave 1–4 migrations; it does not expand `examples/basic-spa`/`examples/basic-ssr` to cover the full migrated set — growing those two apps is task 4.8's job, and every affected item's `examples`/`ssrHydration` dimensions say so explicitly rather than silently passing. Verified: `cargo fmt --all --check`, `cargo check --locked --workspace`, `cargo clippy --locked --workspace --all-targets -- -D warnings`, `cargo test --locked --workspace` (all green, including the 4 new xtask unit tests), `cargo xtask registry validate` (43/43), `cargo xtask provenance check`, and `cargo xtask parity` (37/37, 0 unclassified).
- [x] 4.8a Complete the Tailwind v4 CSS pipeline that M2's `adico init` CSS installer (task 3.11) only half-built: `plan_theme_install` in `packages/adico-cli/src/css.rs` writes the `@theme`/token block but never writes the `@import "tailwindcss";` bootstrap line or a `@source` content-scanning directive, and no installed app links the compiled stylesheet (`document::Stylesheet { href: asset!(...) }`) — found while trying to actually view migrated components styled in a browser, where every installed example rendered as unstyled semantic HTML. Fixed the CSS installer to bootstrap a fresh entry; confirmed the Tailwind *compiler* itself needs a project-root `tailwind.css` as input (distinct from the generated `assets/tailwind.css` output, matching the original M0 design) and a `document::Stylesheet` link plus the `document` Dioxus feature in the consumer's own `Cargo.toml` — verified end to end in both `examples/basic` and `apps/playground` under real `dx serve`, generating real Tailwind utility CSS from actual class usage, not just token files. Also fixed a related `adico add` regression found along the way: re-running add for a new item was silently dropping previously-installed items' `mod.rs` declarations (`packages/adico-cli/src/modules.rs`). Still open: the `Stylesheet` link and `document` feature are manual one-line steps today, not auto-inserted or documented anywhere consumer-facing — fold that into 4.8b's own `adico init` output/README work rather than duplicating it here.
- [x] 4.8b Gave `apps/playground` per-component routing: split the single scrolling `App` gallery into `src/pages.rs` (one page component per currently migrated registry item — badge, button, calendar, card, combobox, context-menu, date-picker, dialog, dropdown-menu, hover-card, input, item, menubar, pagination, popover, select, sheet, sidebar, skeleton, textarea, tooltip; 21 total) plus `src/demo.rs`'s shared `Demo` wrapper. `main.rs` now defines a `#[derive(Routable)] enum Route` (added the `router` Dioxus feature to `Cargo.toml`), a `Layout` component with a nav sidebar (`Link` to every page) wrapping `Outlet::<Route> {}`, and a `Home` landing page. Added `apps/playground/README.md` documenting the still-manual `document::Stylesheet` + `document`-feature + project-root-`tailwind.css` wiring from 4.8a so it isn't lost for future consumers. All components remain installed through the real `adico` CLI (`src/components/` untouched, no direct registry workspace imports). Verified via `cargo check -p adico-playground --features web` (clean) and a real `dx serve --platform web` run: build completed, Tailwind hot-reloaded a real 21KB+ compiled stylesheet, and `curl` confirmed 200s with correct SPA content for `/`, `/button`, and `/sidebar`. Browser-visual confirmation was not possible this session (the `claude-in-chrome` extension reported not connected); still open for a future session to actually click through the nav in a real browser.
- [x] 4.8c Added Storybook-style live prop controls to representative component pages: `apps/playground/src/controls.rs` provides three small bound-control components (`BoolControl` -> checkbox, `TextControl` -> text input, generic `SelectControl<T>` -> `<select>` over a closed set of `(label, value)` options), each writing straight into a page-local `Signal` that the demo's props read from, so a change re-renders immediately with no page reload. `Demo` (`src/demo.rs`) grew an optional `controls: Option<Element>` slot rendered as a strip above the live demo. Wired onto: `ButtonPage` (bool `disabled`), `InputPage` (Wave 1 rep: text `placeholder` + bool `disabled`), `SelectPage` (bool `disabled`), `DialogPage` (bool `open`, sharing the same signal the trigger already used), `PopoverPage` (Wave 3 rep: bool `open` + closed-enum `align: ContentAlign`), `SidebarPage` (Wave 4 rep: closed-enum `collapsible: SidebarCollapsible` + `side: SidebarSide`) — covering button/dialog/select plus one representative from each of Wave 1/3/4. Left every other page's props as fixed example content (children/callbacks aren't meaningfully editable). Verified via `cargo check -p adico-playground --features web` (clean) and a real `dx serve --platform web` run with `curl` 200s on `/button`, `/input`, `/select`, `/dialog`, `/popover`, `/sidebar`; browser-visual confirmation of the controls actually toggling remains open for a future session (`claude-in-chrome` still not connected).
- [x] 4.8d Added a global playground customization launcher (`apps/playground/src/theme.rs`, wired in `main.rs`'s `Layout`) that opens the installed Dialog component (`ui::Dialog`/`DialogOverlay`/`DialogContent`) with independently selectable primary, secondary, and tertiary palette presets (`Palette::{Slate,Blue,Violet,Emerald,Rose,Amber}`) plus a light/dark `Appearance` select. Exposes editable values for the full installed shadcn-style theme contract via four collapsible groups (Surfaces, Roles, Structure, Sidebar) covering all 28 tokens: surfaces/foregrounds, primary/secondary/muted/accent/destructive role pairs, border/input/ring/radius, and all eight sidebar variables — each color token gets a native color-picker input (HSL⇄hex round-trip) plus a raw-value text field, radius gets a plain text field. `generate_theme()` is a dependency-free deterministic LCG-based action (no `js_sys`/browser RNG) that produces a new editable palette combination; `copy_theme_css` is a `#[cfg(target_arch = "wasm32")]`-gated web clipboard action exporting the active appearance as canonical `:root`/`.dark` CSS custom properties (non-wasm targets get a stub message, matching the target-gated adapter pattern used elsewhere). Light and dark customizations are stored as separate `ThemeVariables` (`ThemeSelection::light`/`dark`) so editing one never overwrites the other. The selection is applied once at the `App`/`Layout` shell boundary (`div { style: "{selection.variables()}" }` wrapping `Router::<Route> {}`), and `ThemeVariables::variables()` emits both the canonical `--primary`-style variables and their `--color-primary: hsl(...)`-style Tailwind aliases together, so every routed page reacts without per-component changes. This was implemented and closed under the `improve-existing-components` change (task 1.4, archived at `openspec/changes/archive/2026-08-29-improve-existing-components/`) before this file was updated to reflect it; re-verified in this session: `cargo test -p adico-playground theme::` (7 unit tests covering the full palette matrix, direct-edit isolation between appearances, generated-theme change, HSL/hex round-trip, Tailwind-alias mirroring, and `:root`/`.dark` export shape) passed, `cargo check -p adico-playground --locked --no-default-features --features web --target wasm32-unknown-unknown` passed, and a live `dx serve --platform web` run verified in a real Chrome tab: the Button page (Wave 1) and Sidebar page (Wave 4) both re-render live on primary-palette change, dark-mode toggle, and "Generate theme"; "Copy Dark CSS variables" showed the live "CSS variables copied to the clipboard." status; and the Structure group's direct `--border`/`--input`/`--ring` editors rendered correctly. `cargo xtask registry validate` and `cargo xtask provenance check` both still pass.
- [x] 4.8e The `improve-existing-components` change (archived at `openspec/changes/archive/2026-08-29-improve-existing-components/`, delta spec synced into `openspec/specs/adico-existing-components/spec.md`) already paused further component migration and hardened all 21 then-migrated registry components (Button, Badge, Card, Input, Textarea, Skeleton, Item, Pagination, Dialog, Sheet, Select, Combobox, Tooltip, Popover, Hover Card, Dropdown Menu, Context Menu, Menubar, Calendar, Date Picker, Sidebar) in the consumer-style playground: public composition/API, semantic-token coverage in light/dark/custom themes (via 4.8d's tray), visual states, keyboard/pointer/focus behavior, accessibility, responsive layout, and representative live controls, per its published hardening report (`docs/adico/component-hardening-audit.md`, corrected for internal-consistency issues in a follow-up fix commit). Defects were corrected through shared primitives (e.g. Menubar's dropped `disabled` prop) or copied registry source (e.g. Calendar/Sheet drift between playground and registry, `cn` missing from `select`/`combobox`/`dropdown-menu` registry dependencies), with bounded exceptions recorded explicitly (check/radio/submenu menu-item parts for Dropdown Menu/Context Menu/Menubar — no primitive support to wrap; desktop and SSR-hydration fixtures — none exist repo-wide, applied uniformly rather than per component; visual regression — no tooling yet, tracked separately as M4/`tests/visual` scope). The archived change's final validation table records `cargo fmt --all --check`, `cargo xtask registry build`/`validate`, `cargo xtask provenance check`, `cargo check --locked --workspace`, `cargo clippy --locked --workspace --all-targets -- -D warnings`, `cargo test --locked --workspace`, `adico-primitives`/`adico-playground` web+wasm32/server/desktop feature checks, and `dialog.spec.ts`/`select.spec.ts`/`wave3.spec.ts`/`wave4.spec.ts` Playwright runs (all passed, zero critical axe violations) against freshly reinstalled consumer fixtures via `dx serve`. Re-verified in this session: `cargo fmt --all --check`, `cargo check --locked --workspace`, `cargo xtask registry validate`, and `cargo xtask provenance check` all still pass on the current tree. Wave 2 migration (task 4.5a) may now resume, as recorded in the archive commit message.
- [x] 4.8f Added a cross-platform `theme_mode` module to `adico-primitives`: `ThemeMode::{Light, Dark, System}`, `ResolvedTheme::{Light, Dark}`, and `use_theme_mode()` following the crate's existing `use_controlled` convention. `System` resolves via the `dark-light` crate (v3.0.0, MIT/Apache-2.0, `rust-version = "1.78.0"`, confirmed to compile clean for native, `--no-default-features --features web --target wasm32-unknown-unknown`, and `--features desktop` before adopting it), gated behind this crate's existing `#[cfg(any(feature = "web", feature = "desktop"))]` pattern with a deterministic `Light` fallback otherwise — the same SSR-safety convention used by `dialog`/`portal`. A real, non-obvious defect was found and fixed here purely by running `cargo test --locked --workspace` (not `-p adico-primitives` alone): the crate's own `default = []` features mean `cargo test -p adico-primitives` in isolation gives real feature isolation, but `cargo test --workspace` unifies Cargo features across every workspace member building the same target, and since `examples/basic-spa`/`apps/playground` depend on `adico-primitives` with `features = ["web"]`, that feature is genuinely active during a full workspace test run — the initial "`System` resolves to `Light` without a client feature" test assumed isolation it doesn't get under `--workspace` and flaked (found the real OS was in Dark mode on the dev machine). Fixed by splitting into two `#[cfg]`-gated tests, one per profile, both of which now hold unconditionally. Live OS-theme-change reactivity (`dark-light`'s `subscribe()`/`stream()` APIs) is a named, deliberately deferred v1 gap — resolution is a one-shot `detect()` read, not a live subscription. Verified: `cargo test -p adico-primitives` and `cargo test --locked --workspace` both pass (4 `theme_mode` unit tests either way), plus native/`--features web --target wasm32-unknown-unknown`/`--features desktop` all check and clippy clean.
- [x] 4.8g Added two registry:ui items built on 4.8f: `mode-toggle` (Light/Dark/System selector composing the *installed* `dropdown-menu` registry item — not the raw primitive, matching this repo's dialog-composes-button composition convention — plus a shared `GlobalSignal<ThemeMode>` in `theme_mode` so multiple mode-toggle/theme-switcher instances on one page stay live-synced, not just synced after a reload) and `theme-switcher` (a distilled Slate/Blue/Violet/Emerald/Rose/Amber palette picker recoloring `--primary`/`--primary-foreground` and their Tailwind aliases — productizing the playground's existing preset concept, but not its full 28-token advanced editor, which stays playground-only per design.md §7a). Persistence is `localStorage` on `web` (via a narrowly-scoped `document::eval`, the same mechanism this crate already uses elsewhere) and a JSON preferences file under the OS temp directory on `desktop` (a deliberately simple, named-limitation location — a real per-app data directory would need an extra dependency this crate doesn't otherwise need). `mode-toggle` is classified `EXISTING_SHADCN_EQUIVALENT`-adjacent and got a real `parity.json` entry following task 4.7's honest-evidence convention (`status: "in_progress"`, M4-scope dimensions honestly `false` with a note); `theme-switcher` was added to `adico-xtask`'s `DIOXUS_ONLY_EXTRAS` list (no shadcn equivalent — ui.shadcn.com's theme customizer is a docs-site feature, not a shipped component) and carries no `parity.json` entry, per the Wave 5 extras-labeling mechanism from task 4.6. Both items installed and compiled through the real `adico` binary into a new `tests/installation/theme-consumer` fixture (native + `wasm32-unknown-unknown`), with `tests/playwright/mode-toggle.spec.ts`/`theme-switcher.spec.ts` run live against `dx serve` — zero critical axe violations. A real defect was found and fixed purely by that live run, not by compiling: `theme_mode`'s DOM-class-application effect originally took the resolved `ThemeMode` as a plain copied function argument rather than reading the shared signal inside the effect body, so Dioxus's automatic reactive-dependency tracking never detected a dependency — the code compiled, rendered, and clicking "Dark" silently did nothing to `<html>`'s class after the first mount. Fixed by reading the signal directly inside the effect closure. `cargo xtask parity` now reports 38/38 classified, 6 extras excluded (was 37/5 after task 4.7).
- [x] 4.8 Reinstalled `examples/basic-spa` and `examples/basic-ssr` entirely through the real `adico` binary: cleared each fixture's stale M2-vertical-slice-only install state (their `button.rs`/`card.rs`/`dialog.rs`/`select.rs` had drifted significantly from the current registry source — e.g. `button.rs` was missing `ButtonVariant`/`ButtonSize` entirely — since neither example had been refreshed since M2, unlike `apps/playground` which task 4.8b already brought current) and ran `adico add --all`, landing all 44 current registry:ui items (37 shadcn-equivalent + 5 Wave 5 extras + `mode-toggle`/`theme-switcher` from 4.8f/4.8g) in both, with zero direct registry workspace imports (`grep -rn "registry::"` clean in both). Rewrote both `main.rs` files to exercise a representative sample of the installed set — not one route per component, since these two fixtures are meant to stay lighter-weight "consumer validation fixtures" per `README.md`, unlike the playground's full nav/router treatment — reusing verified-working composition snippets from the existing `tests/installation/wave*-consumer` fixtures rather than inventing new (and therefore unverified) usage for each component. `examples/basic-ssr`'s exact SSR/hydration smoke-check contract (`tests/playwright/fullstack.spec.ts`'s asserted strings/roles for Button/Dialog/Select) was preserved unchanged. Two real defects were found and fixed purely by live `dx serve` runs, not by compiling: (1) the demo's initial Avatar usage (copied from the wave2-state-consumer fixture's deliberately-broken image URL, used there to exercise the fallback state) tripped `fullstack.spec.ts`'s "zero console issues" assertion with a genuine `ERR_NAME_NOT_RESOLVED` network error under real SSR+hydration — fixed by dropping the external image entirely (fallback-only Avatar); (2) `dx serve`'s hot-reload does not reliably patch the fullstack *server* binary on file save (only the client wasm bundle), so the fix above required a full server restart to actually take effect — an environment quirk to know about, not a bug in this fixture. A related, pre-existing, *not newly introduced* defect was also discovered once both huge kitchen-sink demo pages made page-wide accessibility scans impractical: the installed `Tabs` registry component emits an invalid `aria-controls` value under SSR+hydration (`aria-controls="adico-N"` pointing at an id no element in the DOM actually has), a critical axe violation. This is a real, inherited-from-Wave-2 defect, but fixing `tabs.rs` itself is out of this task's scope; it surfaced only because `mode-toggle.spec.ts`/`theme-switcher.spec.ts`'s accessibility checks were (correctly) scoped down to their own `#mode-toggle-demo`/`#theme-switcher-demo` containers via `AxeBuilder.include()` rather than scanning the whole page, which also fixed a real test-design flaw (an unrelated defect anywhere on a shared demo page would otherwise fail every component's accessibility test on that page). The Tabs `aria-controls` bug itself remains a named, open gap for a future M4 hardening pass. No dedicated `docs/adico/` report was written for 4.8/4.8f/4.8g — this cross-cutting task's scope and depth matches 4.8b/4.8c/4.8d (playground refresh work), which were also recorded as task-entry-only rather than a separate wave-style document; the wave-report convention (`m3-wave*-migration.md`) is specifically for M3's numbered migration-queue batches, which this isn't. Verified: `cargo fmt --all --check`, `cargo check --locked --workspace`, `cargo clippy --locked --workspace --all-targets -- -D warnings`, `cargo test --locked --workspace`, `cargo xtask registry validate` (45/45), `cargo xtask provenance check`, `cargo xtask parity` (38/38 classified), `wasm32-unknown-unknown` checks for both examples' `web` feature, a `server`-feature check for `examples/basic-ssr`, and real `dx serve --platform web` runs for both examples with `fullstack.spec.ts` + `mode-toggle.spec.ts` + `theme-switcher.spec.ts` all passing (6/6 against basic-ssr, 5/5 against basic-spa's web-only build), zero critical axe violations.
- [x] 4.8h `adico init` still doesn't make a fresh consumer project ready to add components the way shadcn's `init` does (CSS vars + Tailwind configured, `components/ui` ready to receive installs): `packages/adico-cli/src/init.rs`'s `plan_init`/`InitPlan::apply` creates `src/components`, `src/components/ui`, `src/adico_lib`, `src/hooks`, and the CSS entry's *parent directory*, and writes `components.json` — but never creates or verifies the project-root `tailwind.css` compiler input (with the `@import "tailwindcss";`/`@source`/theme-marker region — that only happens later, per-component, via `add.rs`'s `plan_theme_install`), never creates or verifies `Dioxus.toml`, and never adds or verifies the `document::Stylesheet { href: asset!(...) }` link + `document` Cargo feature in the consumer's entrypoint (confirmed by `grep`: no `Stylesheet`/`asset!`/`document::` reference anywhere in `adico-cli/src`). `examples/basic-ssr` hit exactly this: it had been `adico init`-ed and had components added, but was missing `Dioxus.toml` and the CSS entry/link entirely, so it rendered as fully unstyled HTML despite `cargo xtask registry validate`/`provenance check`/`parity` and the existing Playwright specs all passing — those checks assert on text/role content and manifest completeness, not on rendered styling. Manually fixed for `basic-ssr` only in commit `d901af0` (added `Dioxus.toml`, `tailwind.css`, and the `Stylesheet`/`asset!` wiring by hand); the CLI itself is unchanged and will reproduce this gap on the next fresh `adico init`. **Closed by task 4.8j**: `adico init` now scaffolds `Dioxus.toml` and eagerly bootstraps the project-root `tailwind.css` (via the same `plan_theme_install` the old per-component path used, now invoked unconditionally during init instead of waiting for a themed `add`), and `adico init`/`adico add` both print an explicit one-line instruction naming the exact `document::Stylesheet` code to add when the entrypoint doesn't yet link the compiled stylesheet, rather than reporting `adico is ready`/success while the project is silently unable to render styled output — the "either/or" this task's `Needs` clause allowed, with the warning path chosen deliberately over auto-editing an arbitrary consumer's entrypoint (see 4.8j and `design.md` §7c item 6 for why). Verified end-to-end on a true zero-state fixture (see 4.8j) with the fix; also verified the warning fires correctly on both `adico init` and `adico add` when the entrypoint genuinely lacks the stylesheet link.
- [x] 4.8i `adico add --replace` refused to overwrite a genuinely user-modified file even when explicitly asked to, making the flag unusable for its actual purpose. The 5.2a-era diagnosis (install-time import-path rewriting causing untouched files to never match) did not reproduce under direct testing: `adico-cli` has no import-path-rewriting code at all (confirmed by grep and by reinstalling an untouched file with and without `--replace` in a scratch fixture — both succeeded cleanly, since `existing_checksum == file.checksum` for an untouched file bypasses the modified-file check entirely regardless of the flag). The real bug, reproduced directly: `plan_source_install` in `packages/adico-cli/src/add.rs` gated `--replace`'s overwrite on `installed_checksum == Some(existing_checksum)`, where `installed_checksum` came from `adico.lock`'s previously recorded checksum for that path. That condition only recognizes a narrow "registry version bumped, file on disk is otherwise untouched" case; a real hand-edit (`existing_checksum` differs from both the lock's prior value and the current registry checksum) always failed that equality check and fell through to `Err(AddError::ModifiedConsumerFile)` regardless of `--replace` — reproduced with a scratch fixture: `adico add button` then hand-edit, then `adico add button --replace` still refused. Fixed by removing the `installed_checksum`/lock-comparison gate entirely: `replace: true` now unconditionally allows the overwrite once the plan has already confirmed the on-disk content differs from the current registry source, which is what the flag is documented to mean. `adico add <item>` (no flag) still correctly refuses an untouched-relative-to-registry file that was hand-edited. Added a new test, `replace_overwrites_a_genuinely_user_modified_file`, alongside the existing `modified_source_and_invalid_registry_bytes_fail_before_writing` test it complements. Also fixed a related, smaller doc gap found in the same pass: `adico add --all` works (confirmed) but was missing from `main.rs`'s usage string; added `adico add --all [--dry-run] [--replace]` to it. Verified: `cargo test -p adico-cli` (29/29, including the new test), `cargo fmt --all --check`/`cargo clippy -p adico-cli --all-targets -- -D warnings` clean for the changed files, `cargo xtask registry validate`/`provenance check`/`parity` and `openspec validate --strict` all pass, and both the refuse-without-`--replace` and overwrite-with-`--replace` paths were re-verified live against the installed `adico` binary in a scratch fixture, not just the unit test.
- [x] 4.8j Implemented `adico css build`/`adico css check` (new `packages/adico-cli/src/css_build.rs`) per `design.md` §7c, closing 4.8h with a real command rather than a warning-only fallback. **A deeper, previously-unknown bug was found and fixed along the way**: `default_configuration()`'s `css.entry` was `"assets/tailwind.css"` (the *compiled output* path) instead of `"tailwind.css"` (the project-root *input* `dx serve`/`dx build` actually looks for by convention — confirmed via `dx config schema`'s `tailwind_input`/`tailwind_output` keys and by inspecting `dx`'s own binary strings for its hardcoded Tailwind-detection logic). This meant a truly fresh `adico add` today would have bootstrapped the `@import`/theme-marker region directly into `assets/tailwind.css` — the wrong file — which is why `examples/basic-spa`/`basic-ssr`'s working root `tailwind.css` files only existed because they were hand-created/hand-patched earlier this session (`d901af0`, and an unlogged manual sync during 5.3a), silently working around this bug rather than exercising the real code path. Fixed by correcting `css.entry`'s default and all 19 already-committed `components.json` files (`sed` value-only change, no schema change) to `"tailwind.css"` — `plan_theme_install`'s existing marker-replace logic and `relative_source_directive`'s existing depth calculation both already handled a root-level entry correctly once pointed at it (confirmed via the full `adico-cli` test suite, 28/28 passing, plus a new `official_init_is_reviewable_idempotent_and_preserves_unrelated_files` assertion). Scope delivered: (1) `adico-cli` fetches and caches Tailwind's official standalone native CLI release `v4.1.5` for the host platform into `~/.adico/tools/tailwindcss-v4.1.5/` (mirroring `dx`'s own `~/.dx/tools/` convention so the caches never collide), verified against Tailwind's published `sha256sums.txt` fetched over HTTPS from the same GitHub release (independently cross-checked this session: the macOS arm64 asset's digest matches byte-for-byte the copy `dx` itself had already cached and been using) — zero Node/npm/npx references anywhere in the new code, confirmed by grep. (2) `adico css build` compiles `tailwind.css` → `assets/tailwind.css`; `adico css check` compiles into a scratch temp file and byte-diffs it against the checked-in output, exiting non-zero with a clear stale-file message on drift. (3) `adico init` now scaffolds `Dioxus.toml` (`[application]`/`[web.app]` matching the existing example projects' shape) and eagerly runs `plan_theme_install` so a bare `adico init` alone produces a compilable, styled-ready `tailwind.css` before any component is added; both `init` and `add` invoke `adico css build` as a best-effort step after `apply()` (a compile failure — e.g. no network on first run — warns but does not abort the command, since the underlying install/scaffold already succeeded) and print a one-line `document::Stylesheet` instruction when the entrypoint doesn't yet link the compiled CSS, deliberately not auto-editing the entrypoint (per `design.md` §7c item 6). (4) Decided to keep `examples/`/`apps/playground`'s `assets/tailwind.css` committed with `adico css check` as the validate-gate (matching `registry/generated/*`'s existing convention), not `.gitignore`d — the lower-risk option `design.md` flagged, deferring the gitignore-plus-CI-step alternative as a clean follow-up rather than bundling two risky changes. Verified end-to-end on a true zero-state fixture outside the repo (`/tmp`, not a `tests/installation/*` fixture): `adico init && adico add button` with no manual CSS/Dioxus.toml fixes, followed by a real `dx serve` + a claude-in-chrome screenshot showing a correctly styled bold heading and dark rounded primary button — genuine proof, not a source-level assertion. Also verified `adico css check` fails with a clear message on a deliberately truncated `assets/tailwind.css` and passes again after `adico css build`, and that the warning path fires correctly on both `init` and `add` when the entrypoint genuinely lacks the stylesheet link (separate fixture, no `document::Stylesheet` in `main.rs`). Resyncing the 3 real projects surfaced two more honest findings rather than a clean no-op: `examples/basic-spa`'s committed output already matched a fresh compile exactly (from this session's earlier 5.3a `dx serve` verification), but `apps/playground`'s was missing 143 lines of legitimately-referenced utility classes (never recompiled after 5.3a's registry changes), and `examples/basic-ssr`'s was worse — it still contained the *raw, uncompiled* `@theme {}` marker block sitting inline (a leftover of the original manual `d901af0` hand-fix, not a real Tailwind compile) alongside partial compiled CSS. Both are now genuinely compiled via `adico css build` and pass `adico css check`. Full validation matrix passes: `cargo fmt`, `cargo check --workspace`, `cargo test -p adico-cli -p adico-registry-core --lib` (28 + 20 passing), `cargo xtask registry build/validate`, `provenance check`, `parity`, `openspec validate --strict`. `docs/adico/m0-toolchain-decisions.md`'s Tailwind CLI row and styling-contract section updated to name the actual standalone-binary mechanism instead of the stale npm pin. New workspace dependencies: `dirs = "6.0"` (already resolved transitively, now a direct `adico-cli` dependency) and `reqwest` (already a workspace dependency used elsewhere, now also used by `adico-cli`) — no new crates added to the dependency graph.
- [x] 4.8k Add a `theme-builder` `registry:ui` item per `design.md` §7d: promotes `apps/playground/src/theme.rs`'s genuinely novel logic (the full 28-semantic-token editor with independent light/dark values, deterministic "generate theme," and CSS export) into a real, generic, installable component, since it isn't playground-specific in nature — only its current location is. Applies its edited tokens live through the existing `adico_primitives::theme_mode::apply_root_properties` (already fully generic, no primitive change needed) resolved against `use_persisted_theme_mode()`, the same mechanism `theme-switcher` already uses for its 4 properties, so `theme-builder` composes with `mode-toggle`/`theme-switcher` on one document-root mechanism instead of `theme.rs`'s separate inline-shell-style approach. Classified `EXISTING_DIOXUS_EXTRA` (no shadcn upstream equivalent, added to `DIOXUS_ONLY_EXTRAS`, no `parity.json` entry), adico-original provenance, same treatment as `theme-switcher`. A follow-up change (`2026-08-30-playground-uses-registry-theme-and-sidebar`) then deletes `theme.rs` outright and rewires `apps/playground` onto `mode-toggle`/`theme-switcher`/`theme-builder`/`sidebar` — not part of this task. Delivered as `registry/ui/theme_builder.rs`, porting `theme.rs`'s `ThemeVariables`/palette/`generate_theme`/`css_export` logic verbatim (renamed the light/dark selector from `ThemeMode` to `ThemeAppearance`, local to this file, to avoid confusion with `adico_primitives::theme_mode::ThemeMode`'s `Light`/`Dark`/`System`), plus an `on_theme_change: Callback<ThemeVariables>` prop with `ThemeVariables` made `pub`. One real deviation from `theme.rs`, caught before writing code and confirmed with the user: `theme.rs`'s `copy_theme_css` calls `web_sys`/`wasm_bindgen_futures` clipboard APIs directly, which would leak a browser-interop detail into registry UI source (prohibited by `CLAUDE.md`'s architecture rules) and require new `web-sys`/`wasm-bindgen-futures` cargo dependencies no other registry item needs — `ThemeBuilder` instead renders its CSS export into a read-only, selectable `<textarea>`, still fully paste-ready with zero new browser API surface (recorded in `design.md` §7d). Registered in `registry/registry.json` (checksum `04e2bef992cc8ae352587441d1dd5d1822526a1c67e8a9dcf4a17bcfb744e9ff`) and `packages/adico-cli/src/main.rs`'s embedded-registry `include_bytes!` match arm (a required step this task's own description didn't call out — every embedded registry item needs one, confirmed by the CLI's own "does not embed the requested registry source" error on the first `adico add theme-builder` attempt), added `theme-builder` to `packages/adico-xtask/src/main.rs`'s `DIOXUS_ONLY_EXTRAS`, and added the missing `@adico/theme-builder` entry to `adico-cli`'s own `add_all`-fixture test list (`tests::discovery_uses_default_and_explicit_configured_sources_without_mutation`, which hardcodes the full 46-item catalog and failed until updated). Ported all 7 of `theme.rs`'s existing pure-logic unit tests (token-contract completeness, direct-edit isolation by appearance, generate-theme palette change, HSL/hex round-trip, token-override alias propagation, CSS-export selector/canonical-token check, reset-to-default) — `token_overrides_...`/`css_export_...` reworked to assert against the new `root_property_pairs()` helper (replacing `theme.rs`'s ad hoc `variables()` string-concat, since `apply_root_properties` takes `&[(&str, String)]` pairs directly) rather than string-containment checks. Live-verified end to end via a temporary scratch page (never committed — installed, exercised, then fully reverted with `git checkout`/`rm` so no dead-unused-component drift was left in `apps/playground`, the exact anti-pattern this session's earlier work fixed for `mode-toggle`/`theme-switcher`): palette-preset clicks and "Generate theme" visibly repaint the page and update the CSS-export textarea; `getComputedStyle(document.documentElement).getPropertyValue('--primary')` in the live browser confirmed `apply_root_properties` actually reaches the document root, not just local component state; switching the Appearance select between Light/Dark shows independently-edited token sets and swaps the export's `:root {}`/`.dark {}` selector, matching `theme.rs`'s original behavior exactly. Verified: `cargo fmt --all --check`, `cargo check --locked --workspace`, `cargo clippy` (both the required crate set and `-p adico-playground --features web`) with zero warnings, `cargo test --locked -p adico-cli -p adico-primitives -p adico-registry-core -p adico-test-utils -p adico-xtask` (all passing, including the fixed `add_all` fixture), `cargo test -p adico-playground` (both default and `--features server`) with the 7 new `theme_builder` tests passing during the temporary live-install check, `cargo xtask registry build`/`registry validate`/`provenance check`/`parity` (46 items, 7 extras excluded), `openspec validate build-adico-component-ecosystem --strict`. Filed the CSS-token-contract gap this task's research surfaced (this repo's `css.rs` predates current shadcn.com theming: no `--chart-*`, `--sidebar-background` vs renamed `--sidebar`, single `--radius` vs the expanded scale) as new task 5.3d rather than silently expanding this task's scope to fix it. **Amendment (found and fixed during `2026-08-30-playground-uses-registry-theme-and-sidebar`'s live wiring, not this task's own verification pass):** `ThemeBuilder`'s `use_effect` applied its full 28-token set — including `--background`/`--foreground`, which `mode-toggle`'s `.dark` class selector also defines — on every mount via inline style, with no cleanup on unmount; since an inline style always beats a class selector, mounting `theme-builder` even once permanently broke `mode-toggle`'s Light/Dark toggle for the rest of the session, in any consumer app. Fixed by adding `adico_primitives::theme_mode::clear_root_properties` (new, mirrors `apply_root_properties`'s `#[cfg(feature = "web")]` gating) and calling it from a `use_drop` cleanup in `ThemeBuilder` — see `2026-08-30-playground-uses-registry-theme-and-sidebar/design.md` decision 4a for the full root-cause and fix narrative, and its task 5.2 for live re-verification evidence.
- [x] 4.9 Recorded M3 acceptance in [`docs/adico/m3-acceptance.md`](../../../docs/adico/m3-acceptance.md) (linked from `UPSTREAMS.md`): of the 45 upstream items `inventory.md` classifies, 38 `EXISTING_SHADCN_EQUIVALENT` and 6 `EXISTING_DIOXUS_EXTRA` are "suitable for current reuse" per those classifications; 37 + 5 = 42 are installed as standalone `registry:ui` items, and the two that aren't (`separator`, `form`) have permanent, already-documented blocking exceptions (primitive-only dependency of `sidebar`; no real upstream component to fork) rather than silent gaps. The one remaining classified item, `navbar` (`NEEDS_PARITY_UPDATES`), is out of M3's scope by its own classification and explicitly deferred to M4/M7, per `inventory.md`'s own note. Installability is evidenced by `examples/basic-spa`/`examples/basic-ssr` installing all 45 registry items (the 42 upstream-sourced ones plus `cn`, `mode-toggle`, `theme-switcher`) through the real CLI (task 4.8) and by the Wave 1-5 `tests/installation/*-consumer` fixtures. Named an honest, non-blocking gap rather than claiming it: `apps/playground` still only has the 21 items from its task-4.8e hardening pass and has not been refreshed to the full 45-item set. Verified in this session: `cargo xtask registry validate` (45/45), `cargo xtask provenance check` (8 records, 63 source units), `cargo xtask parity` (38/38 classified, 6 extras excluded, 0 unclassified), `cargo fmt --all --check`, `cargo check --locked --workspace`, `cargo clippy --locked --workspace --all-targets -- -D warnings`, and `cargo test --locked --workspace` all pass.

## 5. M4 — Existing component parity hardening

- [x] 5.1 Refresh the checked-in first-party shadcn snapshot and compare each inherited equivalent against current composition/API, visuals, variants, states, accessibility, keyboard, theme, RTL, responsive, docs, and platform expectations; verify the audit creates evidence-backed parity gaps rather than name-only matches. Delivered as [`docs/adico/m4-parity-audit.md`](../../../docs/adico/m4-parity-audit.md), covering all 38 `parity.json`-tracked components across the `api`/`visual`/`variants`/`states`/`darkMode`/`rtl`/`responsive`/`desktop`/`docs`/`ssrHydration` dimensions M3 deferred to M4 (source/keyboard/accessibility/cli/cargo/web/examples were left as-is where `parity.json` already carried cited evidence from M1–M3). Evidence was gathered per the dimension's actual nature, not uniformly by reading source: `api` was source-diffed against a sparse shallow clone of `shadcn-ui/ui`'s `new-york-v4` style (a deep-dive on `accordion.rs` found three real deviations — `items-center` vs upstream's `items-start`, and entirely missing `focus-visible:ring`/`disabled:*` and `data-[state=...]:animate` classes — then generalized via a repo-wide grep sweep across all 44 `registry/ui/*.rs` files); `visual`/`states` were verified by starting a fresh `dx serve` for `examples/basic-spa` and screenshotting the rendered page via claude-in-chrome, which caught two defects no source-read would have shown: `slider` renders with no visible thumb, and the `label`+`input` pairing collapses to zero visible gap between them (ownership between component vs. example composition left an open question for 5.2, not guessed at). The class-sweep generalization found 13 of 38 components missing a focus-visible ring on a genuinely interactive trigger (`accordion`, `collapsible`, `context-menu`, `hover-card`, `menubar`, `mode-toggle`, `pagination`, `popover`, `sheet`, `tabs`, `tooltip`, plus partial gaps on `dialog`), and 41/44 registry items missing any open/close transition animation (only `alert-dialog`/`dialog`/`sheet` have one) — both cited with exact grep counts, not estimated. `rtl`, `responsive`, and `desktop` were marked `unmeasurable` for all 38 components after confirming, project-wide, that no RTL strategy, no viewport/responsive test harness, and no desktop-target test directory exists at all (desktop *compilation* is exercised via `target_os`/`feature = "desktop"` gates in `adico-primitives`, but nothing renders or interacts on desktop in any test) — recorded as a milestone-level scope question for 5.6 to resolve explicitly rather than silently passed or silently deferred. `docs` was marked `gap` for all 38: every registry item's generated JSON carries only a one-line `documentation.compositionNote`, and `apps/docs/src/main.rs` is a 19-line stub with zero per-component usage/accessibility/keyboard documentation — direct evidence for 5.5, not a rediscovery left to it. `ssrHydration` was marked `unmeasurable` for 35/38 (only `button`/`dialog`/`select` have cited Playwright evidence, and `fullstack.spec.ts` asserts at the page level, not per-component), with `calendar` and `date-picker` flagged by name as the highest-risk untested candidates since both render "today" in a way that's classically SSR/client-divergent. `variants` was left `unmeasurable` uniformly and named as explicit residual scope for 5.3 rather than claimed as a false match, since per-component variant-enum diffing against upstream was out of this pass's budget. Caveats recorded honestly in the audit doc: light-mode was not independently screenshotted per component (only dark mode, the examples' default); the shadcn comparison used current upstream `main` via shallow clone rather than a checkout pinned to the exact historical catalog SHA `ac60ef5c4db4265d71454dd9ecd3f93e255d7211` (no discrepancy observed for what was actually compared, but not assumed away); and `api`/`variants` parity beyond the interaction-state class sweep was not exhaustively diffed component-by-component beyond the `accordion` deep-dive sample. Linked from `UPSTREAMS.md`'s new M4 evidence section. Verified: `openspec validate build-adico-component-ecosystem --strict` passes with this task checked off.
- [x] 5.2a First sub-batch of 5.2: closed the focus-visible/disabled-state interaction-class gaps `docs/adico/m4-parity-audit.md` flagged across 13 components, after re-verifying each one individually against a shallow clone of `shadcn-ui/ui`'s `new-york-v4` style rather than copy-pasting the audit's accordion baseline onto all 13 blind. That per-component check found the audit's grep-signal list included several false positives once composition was accounted for: `collapsible`, `context-menu`, `hover-card`, `popover`, and `tooltip` re-export or thinly wrap their primitive's bare trigger exactly as upstream does (upstream's own `CollapsibleTrigger`/`ContextMenuTrigger`/`HoverCardTrigger`/`PopoverTrigger`/`TooltipTrigger` carry zero default styling, relying on the consumer to compose a styled child) — matches, not gaps, so left untouched; `dialog`/`sheet`'s triggers already compose the installed `Button` component, and `mode-toggle`'s trigger already composes the installed `DropdownMenuTrigger`, both of which already carry a full focus-visible/disabled treatment — also matches, not gaps. `menubar`'s `MenubarTrigger` already had `focus:bg-accent`/`focus:text-accent-foreground` (upstream's own pattern here is a background-color highlight, not a ring) — the real, narrower gap was a missing `data-[state=open]:bg-accent data-[state=open]:text-accent-foreground`, now added. Real, confirmed gaps fixed: `registry/ui/accordion.rs` (`AccordionTrigger`: `items-center` → `items-start` to match upstream's first-line chevron alignment on wrapped labels, plus `rounded-md`/`outline-none`/`focus-visible:border-ring focus-visible:ring-[3px] focus-visible:ring-ring/50`/`disabled:pointer-events-none disabled:opacity-50` added), `registry/ui/tabs.rs` (`TabTrigger`: added `focus-visible:border-ring focus-visible:ring-[3px] focus-visible:ring-ring/50 focus-visible:outline-1 focus-visible:outline-ring`; `disabled:*` was already present), `registry/ui/pagination.rs` (`PaginationLink`: added `outline-none`/`focus-visible:border-ring focus-visible:ring-[3px] focus-visible:ring-ring/50` — upstream reuses `buttonVariants`' focus treatment directly, adico's own anchor had none at all), and `registry/ui/menubar.rs` as above. Also fixed `registry/ui/color_picker.rs`'s `AreaThumb`, a Dioxus-only extra with no shadcn equivalent (not `parity.json`-tracked): its keyboard-focusable drag handle set `outline-none` with nothing replacing it, suppressing the focus indicator entirely — added `focus-visible:ring-[3px] focus-visible:ring-ring/50`. Updated `parity.json`'s `api` dimension evidence/note for `accordion`/`tabs`/`pagination`/`menubar` to cite this fix without flipping `passed` to `true` — the audit's own residual-scope note says full prop-shape/slot composition parity beyond interaction-state classes is still unaudited for these components, so closing the dimension now would overclaim. Verified: `cargo fmt`, `cargo check --workspace`, `cargo xtask registry build` (after recomputing and updating the 5 changed files' checksums in `registry/registry.json` — `registry build` validates authored-source checksums and failed loudly on the first attempt until they were refreshed, exactly as designed), `cargo xtask registry validate`, `cargo xtask provenance check`, and `cargo xtask parity` all pass. `examples/basic-spa`/`basic-ssr` were resynced through the real CLI, not by copy-pasting: found and worked around a real, separate `adico add --replace` bug in doing so — `--replace`'s drift check compares the currently-installed file's checksum against the *pristine* registry checksum recorded in `adico.lock`, but installed files get import-path-rewritten at install time, so an untouched, non-user-modified installed file never matches and `--replace` always refuses it as "user-modified" (not filed as its own task; flagged here for whoever picks up CLI hardening next). Routed around it correctly per the no-copy-paste requirement: removed the 5 affected `adico.lock` entries and vendored files, then ran a plain `adico add` to reinstall them fresh through the CLI. Both examples' `mod.rs` registrations were untouched (identical output), both `cargo check -p adico-example-basic-spa`/`-basic-ssr` pass clean, and `assets/tailwind.css` regenerated to include the new class strings. Live-verified via a fresh `dx serve` + claude-in-chrome: keyboard-focusing the Accordion trigger and the Tabs trigger in `examples/basic-spa` now shows a visible focus ring where none rendered before (screenshots taken via real Tab-key navigation, not just a class-string read — this session's established rule that visual/state dimensions require rendered evidence). The remaining 5.2 gap categories (full API/composition-parity re-audit beyond interaction-state classes, transitions, the slider-thumb bug, label/input spacing, docs, rtl/responsive/desktop scope) are unstarted and left to further 5.2/5.3+ sub-batches.
- [ ] 5.2 Close inherited public composition/API deviations in small dependency-coherent batches and document intentional Dioxus idiom differences; verify affected installed consumer examples compile without exposing primitive internals.
- [x] 5.3a First sub-batch of 5.3: closed the missing open/close transition-animation gap `docs/adico/m4-parity-audit.md` flagged as systemic (`data-[state=...]:animate` classes present in only 3/44 registry items; `animate-in`/`fade-in`/`zoom-in`/`slide-in` entrance/exit classes present in only 1/44). Root-caused why the handful of components that already had these classes (`alert-dialog`, `dialog`, `sheet`) worked at all while adding the same classes to others would not: `tailwind.css`'s managed theme region never defined the `animate-in`/`animate-out`/`fade-*`/`zoom-*`/`slide-*` utilities or the `enter`/`exit` keyframes those classes resolve to — Tailwind v4's `@utility`/`@theme` machinery silently drops undefined utility classes rather than erroring, so `dialog`/`sheet` were themselves relying on hand-authored ad-hoc CSS specific to those two components, not a shared utility layer. Ported the missing utilities from `tw-animate-css` (MIT, upstream revision `1fc30d2b5ad6ab18baf7897828e44ab20a0a62ed`) into `packages/adico-cli/src/css.rs`'s `ANIMATION_UTILITIES_CSS` constant, written into every consumer's managed theme marker region by the existing `plan_theme_install` mechanism (provenance recorded in `provenance/records/adico-cli-theme-animation-utilities.json`; upstream `LICENSE-MIT` retained in `third_party/tw-animate-css/`). Deliberately narrowed to only the utilities adico's registry actually references (`fade-in(-*)`/`fade-out(-*)`/`zoom-in(-*)`/`zoom-out(-*)`/`slide-in-from-*`/`slide-out-to-*`, the shared `enter`/`exit` keyframes and `--tw-enter-*`/`--tw-exit-*` custom properties) — upstream's `blur-in`/`spin-in`/`spin-out`, RTL-aware `slide-in-from-start/end` variants, `collapsible-down/up` keyframes, `caret-blink`, and the timing-control utilities were left out as unused, recorded as a deliberate scope narrowing in the provenance record rather than silently dropped. Added the verified `data-[state=...]:animate-in/animate-out`/`fade-*`/`zoom-*`/`slide-*` classes (each diffed per-component against a shallow clone of `shadcn-ui/ui`'s `new-york-v4` style, not one template copy-pasted onto every component — e.g. `menubar`'s content only got an open-transition because upstream's own menubar omits an explicit close-transition class too) to `registry/ui/context_menu.rs`, `registry/ui/hover_card.rs`, `registry/ui/menubar.rs`, `registry/ui/popover.rs`, and `registry/ui/tooltip.rs`. Explicitly tried and reverted `accordion`'s `data-[state=...]:animate-accordion-down/-up`: adico's `AccordionContent` primitive never sets a measured content-height custom property the way Radix's implementation does, so the keyframe's `height: auto` fallback does not interpolate in Chromium — live-verified via `dx serve` that adding it left the content permanently stuck at `height: 0` (invisible) once "open," a regression, not a fix. Left `accordion`'s animation gap open and documented in `css.rs` as blocked on primitive-layer work (JS-measured height tracking or a CSS grid `0fr`/`1fr` animatable-track restructure) — named as 5.4 scope, not silently dropped. Verified: `cargo fmt`, `cargo check --workspace` (root + `examples/basic-spa`/`basic-ssr`), `cargo xtask registry build`/`registry validate`/`provenance check`/`parity` all pass; reinstalled the 5 fixed components into both examples through the real `adico` CLI (routing around the known `--replace` checksum bug tracked as task 4.8i, same workaround as 5.2a). Live-verified via a fresh `dx serve` + browser JS: clicking Popover's trigger and reading `getComputedStyle` on the resulting `[data-state="open"]` content element shows `animationName: "enter"`, `animationDuration: "0.15s"` — the classes resolve to real, running CSS, not dead class names. The other 4 fixed components were not independently re-screenshotted in this pass (same shared CSS mechanism, verified once); `parity.json`'s `visual` dimension was updated to `passed: true` for `context-menu`/`hover-card`/`menubar`/`popover`/`tooltip` citing this fix specifically, with an honest note that broader visual-baseline/regression-fixture coverage (`tests/visual`) remains open scope, and that `states`/`variants` for these components are untouched (not claimed as fixed by this change). Remaining 5.3 scope: the `accordion` transition (blocked on 5.4 primitive work above), the `slider` no-visible-thumb bug, the `label`/`input` spacing collapse, per-component `variants`/`states` audits, dark-mode light-mode re-verification, and the rtl/responsive/desktop milestone-scope question for 5.6.
- [x] 5.3b Second sub-batch of 5.3: closed the `slider` "no visible thumb" defect and settled the `label`/`input` spacing-collapse ownership question, both flagged by `docs/adico/m4-parity-audit.md` as concrete live-rendering gaps. Root-caused the slider bug: `registry/ui/slider.rs`'s `SliderTrack` set `overflow-hidden`, but adico's composition model nests `SliderThumb` *inside* `SliderTrack` as a DOM child (unlike upstream shadcn/Radix, where the thumb is a sibling of the track, both children of the slider root — confirmed by reading `adico-primitives::slider`'s own doc examples) — the thumb's `size-4` (16px) handle is deliberately larger than the track's `h-1.5`/`w-1.5` (6px) cross-axis size, so `overflow-hidden` silently clipped it to invisible on every slider instance. Fixed by removing `overflow-hidden` from `SliderTrack` and adding `rounded-full` directly to `SliderRange` instead, so the filled portion keeps pill-shaped ends without relying on the track clipping it. Live-verified via `dx serve` + browser JS bounding-rect measurement (not just a class-string read): `getComputedStyle(track).overflow` is now `"visible"`, and the thumb's 16px-tall `getBoundingClientRect()` renders centered on and overflowing the 6px-tall track, matching the intended shadcn look. For `label`/`input`: read both `registry/ui/label.rs` and `registry/ui/input.rs` and confirmed neither carries a hidden margin/gap in its default class string, matching upstream shadcn's own `Label`/`Input` source exactly — upstream's own docs demo composes them inside a caller-owned `<div className="grid gap-1.5">` wrapper, meaning the gap is genuinely a composition responsibility, not a component defect. Settled ownership as "examples, not components" and fixed the three affected consumer usages accordingly (not the registry source): `examples/basic-spa/src/main.rs` and `examples/basic-ssr/src/main.rs` (also swapped `basic-spa`'s raw unstyled `input` for the installed `components::ui::Input`, since `Input` was already installed there), and `tests/installation/wave2-simple-consumer/src/main.rs` (kept the raw `input` there since `Input` isn't installed in that fixture; only added the missing wrapper). Updated `parity.json`'s `slider` entry (`visual`/`states` evidence, still `passed: false` — an honest partial fix, not a dimension-closing claim, since variants/api/dark-mode/etc. remain unaudited for `slider`) but did not add `label`/`input` parity entries since the fix lives in example composition, not registry source. Verified: `cargo fmt --all --check` (confirmed the 10 pre-existing non-canonical `examples/basic-spa` files flagged by an earlier session's clean-slate-rebuild finding are unrelated to and unchanged by this batch — none of today's touched files appear in the fmt-check output), `cargo check --workspace`, `cargo xtask registry build` (after updating `slider`'s checksum in `registry/registry.json` to match the source edit) / `registry validate` / `provenance check` / `parity`, `openspec validate --strict`. Reinstalled the fixed `slider` into both examples via the real CLI (`adico add slider --replace`, using the now-working 4.8i fix directly rather than routing around it) and regenerated `assets/tailwind.css` via `adico css build` for both. The pre-existing `examples/basic-spa` registry-fmt-drift finding (10 files, tracked since the earlier clean-slate rebuild) remains open and untouched — out of scope for this batch. Remaining 5.3 scope: the `accordion` transition (blocked on 5.4 primitive work, per 5.3a), per-component `variants`/`states` audits, dark-mode/light-mode re-verification, and the rtl/responsive/desktop milestone-scope question for 5.6.
- [x] 5.3c Closed the `examples/basic-spa` registry-fmt-drift finding left open by 5.3b (and originally surfaced as a workaround note in 5.2a): `registry/ui/*.rs` (and `registry/lib/*.rs`) are not Cargo workspace members, so `cargo fmt --all` never checks or reformats them, letting registry source drift from canonical rustfmt style unnoticed — a fresh `adico add` of a drifted item then fails the *consumer* project's own `cargo fmt --all --check`, exactly the baseline check `CLAUDE.md` requires for every change. Confirmed via `git stash` that this was a live, already-broken baseline on `main` (not something introduced this session): `cargo fmt --all --check` at the pre-5.3c HEAD failed on 10 already-committed `examples/basic-spa` files (`accordion`, `alert_dialog`, `avatar`, `color_picker`, `drag_and_drop_list`, `progress`, `tabs`, `tag_group`, `toast`, `toggle_group`) — the exact same 10 files flagged as a side-observation during 5.2a and 5.3b, now traced to their actual root: the underlying `registry/ui/*.rs` source itself, not anything consumer-side (confirmed by running `rustfmt --edition 2024 --check` directly against every `registry/ui/*.rs`/`registry/lib/*.rs` file, independent of any consumer project). Fixed the root cause, not the symptom: ran `rustfmt --edition 2024` on all 10 non-canonical `registry/ui/*.rs` files directly (recomputing and updating their checksums in `registry/registry.json`, same mechanical step as 5.2a/5.3b), rebuilt/reinstalled the `adico` binary (its embedded registry is compiled in, so the previous binary still served the pre-fix bytes), then reinstalled all 10 into both `examples/basic-spa` and `examples/basic-ssr` via `adico add <item> --replace` (this repo's real consumer path, not a copy-paste) — `basic-ssr`'s copies were already canonical and came back byte-identical, confirming the drift was `basic-spa`-only. Added a **prevention gate** so this can't silently recur: `cargo xtask registry validate` now also calls a new `check_registry_source_formatting` (`packages/adico-xtask/src/main.rs`) that shells out to `rustfmt --edition 2024 --check` against every `.rs` file the official `registry.json` manifest declares, failing loudly with the offending path(s) and a fix command if any drift is found — deliberately checking the *registry* source directly (not relying on any one consumer project happening to catch it), matching this task's own root-cause finding. Verified the gate actually gates: reproduced the original bug by re-introducing a one-line non-canonical edit into `registry/ui/progress.rs` and confirmed `cargo xtask registry validate` now fails with a clear message naming that exact file, then restored it and confirmed the command passes again. Verified: `cargo fmt --all --check` now passes clean across the *entire* workspace (previously silently broken on `main`), `cargo check --workspace`, `cargo clippy -p adico-cli -p adico-primitives -p adico-registry-core -p adico-test-utils -p adico-xtask --all-targets -- -D warnings`, `cargo test` (same suites as prior batches, all passing), `cargo xtask registry build`/`registry validate`/`provenance check`/`parity`, `openspec validate --strict`. No `registry/ui/*.rs` semantic content changed — every edit here is whitespace/formatting-only (rustfmt output), so no behavior, class string, or API changed for any of the 10 components.
- [ ] 5.3d A real gap found while implementing task 4.8k (`theme-builder`): checked this repo's installed CSS/theme contract (`packages/adico-cli/src/css.rs`, what `adico init` writes and every registry component's Tailwind classes resolve against) against the current `https://ui.shadcn.com/docs/theming` reference and confirmed it predates upstream in three ways — no `--chart-1`..`--chart-5` tokens exist anywhere in this repo; the sidebar surface token is `--sidebar-background` here vs upstream's renamed `--sidebar`; and this repo has only a single `--radius` (with Tailwind aliases `--radius-sm`/`-md`/`-lg` computed from it) vs upstream's expanded `--radius-sm`/`-md`/`-lg`/`-xl`/`-2xl`/`-3xl`/`-4xl` scale. `--destructive-foreground` is also still defined here where upstream dropped it (kept deliberately for now — no consumer-visible break either way). `theme-builder`/`theme-switcher`/`mode-toggle` all correctly cover every token this repo's actual installed contract defines, so 4.8k itself needed no change; this is a separate, larger semantic-token gap in `css.rs` itself, affecting every registry item's Tailwind classes, the `tailwind.css` marker region, and all three theme components' docs — refresh the installed contract to the current tokens (or record a deliberate, evidenced decision not to) as a dedicated 5.3 sub-batch; verify `cargo xtask registry validate`/`provenance check`/`parity` and a live `dx serve` re-check of `theme-builder`'s CSS export against the refreshed contract.
- [ ] 5.3 Close inherited visual, variant, state, semantic-token, dark-mode, RTL, and responsive gaps in small component batches; verify visual and targeted layout fixtures update with approved baselines.
- [ ] 5.4 Close inherited keyboard, focus, ARIA, layering, and pointer interaction gaps by strengthening shared primitives before dependent UI source; verify browser/accessibility tests cover the declared behavior.
- [ ] 5.5 Complete per-component documentation, composition examples, accessibility/keyboard notes, and parity evidence for each hardened batch; verify no component is marked complete with an omitted required dimension.
- [ ] 5.6 Record M4 acceptance: all M3 shadcn-equivalent components have either complete required parity dimensions or visible, justified in-progress status with a dependency-group task; verify `cargo xtask parity` and all targeted checks pass.

## 6. M5 — Low-complexity current shadcn gaps

- [ ] 6.1 Refresh the upstream catalog snapshot and derive the actual low-complexity missing set after M4; verify the proposed batch excludes components blocked by new foundational primitive work.
- [ ] 6.2 Implement the first audited compositional batch (for example Alert, Breadcrumb, Button Group, Empty, Input Group, Kbd, Native Select, Spinner, Table, and Typography only if still missing) as source-owned registry items; verify each installs, compiles, is documented, and has parity entries.
- [ ] 6.3 Implement any shared low-complexity utility/theme requirements as source-installed libraries rather than hidden styled runtime APIs; verify installed source remains understandable and independently editable.
- [ ] 6.4 Add focused visual/accessibility tests and `examples/basic-spa`/`examples/basic-ssr`/`tests/installation/*` coverage for each low-complexity batch; verify each item’s required parity dimensions are recorded.
- [ ] 6.5 Record M5 acceptance: the actual low-complexity gap batch is either complete or reclassified with a documented primitive dependency; verify the refreshed parity report has no silent omissions.

## 7. M6 — Shared primitive expansion

- [ ] 7.1 Use the current gap dependency graph, the 2026-08-30 shadcn props parity audit (`parity.json`'s `api` dimension), and the Base UI (base-ui.com) component/primitive inventory to prioritize direction/RTL context, field semantics, focus/layering, positioning, a unified menu primitive, roving focus, typeahead, drag gestures, observers, and scroll utilities; verify every planned primitive names the components it unblocks, including which existing components require migration (see design.md §8a).
- [x] 7.2 Promote the crate's existing crate-private shared primitives to a documented public surface without behavior change: `use_unique_id`/`use_id_or`, `use_animated_open` (presence), `use_focus_trap`/`FocusTrapScript`, `use_outside_dismiss`/`use_global_escape_listener`, and the `collection`, `selection`/`selectable`/`listbox`, `portal`, and `pointer`/`move_interaction` modules; verify existing consumers (accordion, dialog, alert-dialog, popover, context-menu, select, combobox, tabs, calendar, menubar, toolbar, slider, color-picker, drag-and-drop-list) compile unchanged against the newly public API and no private-module import paths remain. Done 2026-08-31: also promoted `Controlled<T>` (lib.rs), required for `selectable::use_selectable_root`'s newly-public signature to avoid a `private_interfaces` failure under `-D warnings`; added doc comments to every promoted item and module-level docs to `portal.rs`/`pointer.rs`/`move_interaction.rs` (previously undocumented). Deliberately left private (no signature requires promotion, and each is scoped to future 7.3/7.4 work): `EscapeListenerStack`, `FOCUS_TRAP_JS`, `use_effect_cleanup`/`use_effect_with_cleanup`, `time`, `r#virtual`. Verified: `cargo fmt --all --check`, `cargo check -p adico-primitives` on default/`web`/`desktop` features and `--target wasm32-unknown-unknown`, `cargo clippy --locked -p adico-cli -p adico-primitives -p adico-registry-core -p adico-test-utils -p adico-xtask --all-targets -- -D warnings`, `cargo test --locked` for the same five crates (all passing, including all 90 `adico-primitives` doctests and `tests/public_api.rs`). Not run this batch: `registry build/validate`, `provenance check`, `parity`, Playwright — no registry item's source or metadata changed (only `adico-primitives`' own visibility), so these are deferred to when a registry component actually migrates onto the newly-public API (task 7.8). Follow-up same day: renamed the `desktop` Cargo feature to `native` (see design.md §8a) — `packages/adico-primitives/Cargo.toml` and all ~61 `#[cfg(feature = "desktop")]` gates across `avatar.rs`, `checkbox.rs`, `context_menu.rs`, `date_picker.rs`, `drag_and_drop_list.rs`, `lib.rs`, `pointer.rs`, and `theme_mode.rs`, plus their prose doc comments referencing the old name. No registry item, generated consumer `Cargo.toml`, or `dioxus`-crate feature was touched or needed to be: `registry.json` unconditionally pins `features = ["web"]` for every component today (a separate, pre-existing gap) and never referenced `desktop`; `apps/playground`/`examples/basic-spa`'s own `desktop = ["dioxus/desktop"]` feature is Dioxus's own platform-selection feature, a distinct concern from this crate's interop-capability flag. Re-verified after the rename: `cargo fmt --all --check`; `cargo check -p adico-primitives` on default/`web`/`native` features and `--target wasm32-unknown-unknown`; `cargo clippy --locked -p adico-cli -p adico-primitives -p adico-registry-core -p adico-test-utils -p adico-xtask --all-targets -- -D warnings` plus explicit `--features web` and `--features native` clippy passes on `adico-primitives`; `cargo test --locked` for the same five crates and again with `-p adico-primitives --features native` (all passing).
- [x] 7.3a Implement a `direction` primitive: a `Direction::{Ltr, Rtl}` enum, a provider context, and a `use_direction()` hook that defaults to `Ltr` when no provider is present (matching Base UI's Direction Provider); new primitives in 7.3b–7.3d and beyond read direction through this hook rather than inventing their own. `use_controlled` remains the uniform controllable-state pattern this and every other new primitive in §7 builds on — no separate deliverable, just the constraint. Verify: unit tests on the context alone (default when absent, provider present, nested override) — no consumer rewiring in this task. Done 2026-08-31: added `packages/adico-primitives/src/direction.rs` (`Direction`, `DirectionProvider`, `use_direction`) and registered it in `lib.rs`; no existing component reads it yet (deliberately deferred to 7.3b+/7.8). Verified: `cargo fmt --all --check`; `cargo clippy --locked -p adico-cli -p adico-primitives -p adico-registry-core -p adico-test-utils -p adico-xtask --all-targets -- -D warnings` plus explicit `--features web`/`--features native` passes on `adico-primitives`; `cargo check --target wasm32-unknown-unknown -p adico-primitives --features web`; `cargo test --locked` for the same five crates (3 new unit tests — default-without-provider, provider override, nested-provider-scopes-to-its-own-subtree, all via `dioxus_ssr`-rendered HTML assertions matching this crate's established test convention — plus 1 new doctest, all passing).
- [x] 7.3b Extend `collection.rs`'s roving-focus with an `orientation` (horizontal/vertical) concept and direction-aware `focus_next`/`focus_prev` (an RTL flip when horizontal, via 7.3a's `use_direction()`), keeping the existing `Orientation` internal to `CollectionState` rather than changing any registry component's public `horizontal: bool` prop shape (that prop-shape decision belongs to task 7.8's migration work, not here). Also note the same RTL gap in `move_interaction.rs:39`'s `MoveEvent::from_keyboard` (hardcoded `ArrowLeft`/`ArrowRight` mapping, used by slider/color-picker) and decide there whether the flip lives in `from_keyboard` or its caller, for consistency with this task's direction-aware collection. Verify: unit tests directly on `CollectionState`/the pure `next_index_after`/`prev_index_before` functions (collection.rs:628/641) for both orientations and both directions — no VirtualDom required. Done 2026-08-31: added `Orientation::{Vertical, Horizontal}` and `CollectionState::navigate_key(key, orientation, direction) -> bool` (collection.rs) as one shared arrow/Home/End handler, flipping ArrowLeft/Right under `Orientation::Horizontal` + `Direction::Rtl` — not wired into any registry component yet (deferred to 7.8), so `accordion.rs`/`tabs.rs`/`menubar.rs`/`toolbar.rs` keep their own hand-rolled match arms and `horizontal: bool` props unchanged for now. Decided the `move_interaction.rs:39` question: the flip lives in `MoveEvent::from_keyboard` itself, which now takes a `Direction` parameter — required updating its 4 real call sites (`slider.rs`'s `SliderThumb`, `color_picker.rs`'s `AreaThumb`/`AreaThumbSaturationInput`/`AreaThumbValueInput`) to read `use_direction()` and pass it through; this is signature-completion, not the architectural migration 7.8 covers. Verified: `cargo fmt --all --check`; `cargo clippy --locked -p adico-cli -p adico-primitives -p adico-registry-core -p adico-test-utils -p adico-xtask --all-targets -- -D warnings` plus explicit `--features web`/`--features native` on `adico-primitives`; `cargo check --target wasm32-unknown-unknown -p adico-primitives --features web`; `cargo test --locked` for the same five crates and again with `-p adico-primitives --features native` (9 new unit tests — 8 for `navigate_key` covering both orientations, RTL flip, Home/End, and an unhandled-key case, rendered via the crate's established `VirtualDom`+`dioxus_ssr` convention since `CollectionState` needs a live component scope to construct signals; 1 for `from_keyboard`'s RTL flip — all passing).
- [x] 7.3c Extract a shared `use_typeahead` primitive from `select/text_search.rs`'s fuzzy-match engine and `select/context.rs`'s buffer state (currently welded together and only reachable through `select`), and migrate `select` itself onto the extracted version. Verify: the existing `tests/select_typeahead.rs` fixture passes unchanged as the regression oracle: the extraction must not change `select`'s observable typeahead behavior. Done 2026-08-31: moved `select/text_search.rs` (deleted) to `packages/adico-primitives/src/typeahead.rs` unchanged except promoting its `pub(super)` matching functions to `pub`, and added a new `Typeahead` handle + `use_typeahead()` hook wrapping the buffer/clear-task/adaptive-keyboard state that previously lived directly on `select::context::SelectContext` (4 fields collapsed to 1: `typeahead: Typeahead`). Migrated `select/components/select.rs` (`use_select_root`), `select/context.rs` (`add_to_typeahead_buffer`/`learn_from_keyboard_event` now delegate to the handle), and `select/components/list.rs` (one `ctx.typeahead_buffer.take()` call site became `ctx.typeahead.clear()`, which also now cancels the pending auto-clear task on arrow-key nav — strictly safer, not an observable behavior change: the buffer's contents at any query point are identical either way) onto it. `select::test_support::typeahead_best_match` (the `#[doc(hidden)]`, test-utils-gated function `tests/select_typeahead.rs` calls through `adico-test-utils`) now imports from `crate::typeahead` instead of `super::text_search`, same signature. Verified: the regression oracle itself (`cargo test -p adico-test-utils --test select_typeahead`) passes unchanged; `cargo fmt --all --check`; `cargo clippy --locked -p adico-cli -p adico-primitives -p adico-registry-core -p adico-test-utils -p adico-xtask --all-targets -- -D warnings` plus explicit `--features web`/`--features native` on `adico-primitives`; `cargo check --target wasm32-unknown-unknown -p adico-primitives --features web`; `cargo test --locked` for the same five crates and again with `-p adico-primitives --features native` (2 new unit tests directly on the `Typeahead` handle — buffer accumulation across calls, `clear()` resetting the sequence — plus all pre-existing `typeahead.rs` matching-algorithm tests carried over unchanged, all passing).
- [x] 7.3d Implement 2-D/grid keyboard navigation on `collection.rs` (today's roving focus is 1-D only); calendar's day grid is the only current consumer. Verify: unit tests on the grid-navigation math (row/column wrap, RTL column-flip via 7.3a) independent of `calendar.rs`. Done 2026-08-31: added `CollectionState::navigate_grid_key(key, columns, direction) -> bool`, treating items as a `columns`-wide row-major grid; `ArrowUp`/`ArrowDown` move by one row (±`columns`), `ArrowLeft`/`ArrowRight` move by one column (flipped for `Direction::Rtl`), `Home`/`End` go to the absolute first/last item (not row-relative — kept consistent with `navigate_key`'s existing convention). Correction to this task's own premise: `calendar.rs` does **not** use `CollectionState` at all today — it tracks `focused_date: Signal<Option<Date>>` directly and does its own date arithmetic (`previous_day()`/`saturating_sub(7.days())`/etc.), so this is a new capability with no current consumer, not a refactor of an existing one; wiring calendar onto it (if ever done) is 7.8 migration scope, not assumed here. Deliberately no wraparound and no skip-past-disabled-cells (unlike 1-D `navigate_key`): an out-of-bounds or disabled target is dropped and focus stays put, since grids don't auto-skip the way a roving list does. Verified: `cargo fmt --all --check`; `cargo clippy --locked -p adico-cli -p adico-primitives -p adico-registry-core -p adico-test-utils -p adico-xtask --all-targets -- -D warnings` plus explicit `--features web`/`--features native` on `adico-primitives`; `cargo check --target wasm32-unknown-unknown -p adico-primitives --features web`; `cargo test --locked` for the same five crates and again with `-p adico-primitives --features native` (8 new unit tests on a 3x3 grid fixture — row/column moves both directions, RTL column-flip, top-row boundary no-op, a disabled-cell no-op, Home/End, and `columns: 0` returning unhandled — all passing).
- [ ] 7.3 Direction/RTL and extended roving-focus/typeahead primitives (7.3a–7.3d above) are in place; verify unit and browser keyboard fixtures cover LTR/RTL and controlled/uncontrolled behavior once accordion, calendar, menubar, and toolbar are migrated onto them (task 7.8) and the unified menu (task 7.6) exists to exercise them end-to-end — this umbrella task is not satisfied by 7.3a–7.3d alone.
- [x] 7.4a Implement a unified dismissable-layer abstraction merging `use_outside_dismiss` and `use_global_escape_listener` into one layer registry, deciding explicitly whether outside-dismiss becomes stack-ordered (topmost layer only) to match the existing Escape stack's LIFO behavior, or whether both dismissal modes must coexist — check whether any current consumer (dialog, alert-dialog, popover, context-menu) relies on every open layer receiving outside-dismiss simultaneously before deciding. Verify: unit tests directly on the registry's ordering (push/pop/topmost-only-dismissed) using the crate's established `VirtualDom`+SSR test convention; existing consumers of the two merged hooks compile and keep their current observable behavior (no browser suite required for this sub-task — that gap is recorded once in 7.4's closing note). Done 2026-08-31: generalized `lib.rs`'s Escape-only `EscapeListenerStack` into `DismissableLayerStack` plus a `DismissableLayer` handle (`scope_id` + shared stack) and a `use_dismissable_layer()` hook that both `use_global_escape_listener` and `use_outside_dismiss` now call — a component calling both (dialog, popover) registers exactly one layer, not two, since the hook checks `contains()` before pushing and each hook's own `use_drop` removal is idempotent. Decision: made outside-dismiss stack-ordered (topmost layer only), matching Escape. Checked all four real consumers (`alert_dialog.rs` escape-only, `context_menu.rs` outside-dismiss-only, `dialog.rs` and `popover.rs` both) — none nests two `use_outside_dismiss` layers today, so this changes no currently-observed behavior; it only changes future compositions (e.g. a popover portaled on top of a dialog) from "every open layer dismisses on any outside click" to "only the topmost layer dismisses," which is the correct, intentional tightening this task called for. **Correction (2026-08-31, discovered while implementing 7.4d):** the "changes no observed behavior today" framing above is true for the wrong reason. It is not just that no consumer nests two `use_outside_dismiss` layers — `provenance/records/adico-primitives-wave3-overlays.json` and `tasks.md` 4.5a/4.6 already document, from real browser + Playwright testing, that this exact long-lived repeatedly-firing `document::eval` listener pattern (shared by `use_global_escape_listener`, now `use_escape_key`'s predecessor, and `use_outside_dismiss`) never registers `addEventListener` in this Dioxus 0.7.9/0.7.10 web runtime at all. So on `web`, neither channel fired before this task, regardless of layering; this task's stack-ordering logic is correct Rust but was, and for `use_outside_dismiss` still is, sitting on a channel confirmed dead on `web`. See 7.4d for the fix applied to the Escape half. Verified: `cargo fmt --all --check`; `cargo check --locked --workspace`; `cargo check -p adico-primitives` on default/`web`/`native` features and `--target wasm32-unknown-unknown --features web`; `cargo clippy --locked -p adico-cli -p adico-primitives -p adico-registry-core -p adico-test-utils -p adico-xtask --all-targets -- -D warnings` plus explicit `--features web`/`--features native` clippy passes on `adico-primitives`; `cargo test --locked` for the same five crates and again with `-p adico-primitives --features native` (5 new unit tests in `lib.rs`'s `dismissable_layer_tests` — lone layer is topmost, most-recently-registered layer is topmost among two and among three nested, removing the topmost restores the next, and calling the hook twice in one component registers exactly one layer — plus all 91/92 pre-existing doctests and the `select_typeahead` regression oracle, all passing). Not run this batch: `tests/playwright/` — no consumer's real nested-overlay DOM composition changed, so the stack-ordering change for outside-dismiss is unit-verified only; recorded as the standing caveat for all of §7.4 in its closing umbrella task.
- [x] 7.4b Generalize the existing Escape-only `EscapeListenerStack` (lib.rs) into a real z-order layer stack that also tracks topmost/ordering for non-Escape purposes (e.g. which layer's backdrop should sit above the others), building on 7.4a's registry rather than duplicating it. Verify: unit tests on push/pop/topmost ordering with two or more nested layers; note plainly that the resulting visual z-order is only verifiable in a real browser, which this task does not exercise. Done 2026-08-31: promoted 7.4a's private `lib.rs` stack into a new public `packages/adico-primitives/src/layer.rs` module (`Layer`, `use_layer()`), so it's an independently-documented, independently-testable primitive rather than an internal implementation detail — `use_global_escape_listener` and `use_outside_dismiss` now call `layer::use_layer()` directly, replacing the old private `DismissableLayer`/`use_dismissable_layer` (deleted, no behavior change from 7.4a). Added `Layer::depth()` (position from the bottom of the stack) and `Layer::z_index()` (`1000 + depth`), giving overlay components a concrete stacking value to apply to their own root/backdrop elements once they adopt it — no registry component consumes `z_index()`/`depth()` yet, that migration is 7.8 scope. Removed now-unused `RefCell`/`Rc`/`current_scope_id`/`dioxus::core::use_drop` imports from `lib.rs`. Verified: `cargo fmt --all --check`; `cargo check --locked --workspace`; `cargo check -p adico-primitives` on default/`web`/`native` features and `--target wasm32-unknown-unknown --features web`; `cargo clippy --locked -p adico-cli -p adico-primitives -p adico-registry-core -p adico-test-utils -p adico-xtask --all-targets -- -D warnings` plus explicit `--features web`/`--features native` on `adico-primitives`; `cargo test --locked` for the same five crates and again with `-p adico-primitives --features native` (4 new unit tests in `layer.rs` — lone layer at depth 0, three nested layers with increasing depth/z-index and only-innermost-topmost, removing the topmost restores the next one's topmost status, and the duplicate-hook-call-registers-one-layer case carried over from 7.4a — all passing). As noted in the task itself: the resulting `z_index()` values are not yet wired into any component's CSS, so visual stacking correctness remains unverified without a browser; this is folded into 7.4's standing closing-note caveat, not re-recorded per sub-task. **Correction (2026-08-31, see 7.4a's own correction and 7.4d):** the stack this module wraps was, and for `use_outside_dismiss` still is, fed by a dismiss channel confirmed non-functional on `web`; this task's `Layer`/`use_layer()` primitive itself is sound Rust, but only became load-bearing on `web` once 7.4d gave `use_escape_key` a real (non-`eval`) firing mechanism.
- [x] 7.4c Implement a shared, reference-counted scroll-lock primitive (multiple overlays may request a lock; body scroll is only unlocked once every requester has released it), replacing `context_menu.rs`'s private file-local scroll-lock implementation. Verify: the refcount/request-release logic in Rust unit tests; note that the actual `document::eval` body-style effect is unverified without a browser. Done 2026-08-31: added a new public `packages/adico-primitives/src/scroll_lock.rs` (`use_scroll_lock(active)`), a Radix/shadcn-style `document.body.style.overflow` lock, reference-counted via a shared `Rc<Cell<usize>>` context so several overlays open at once only unlock on the last release. Explicit scope decision, documented in the module doc: this does **not** replace `context_menu.rs`'s scroll-suppression — that menu is `position: fixed` and pinned to a click point, and its own code comment already explains it deliberately avoids `overflow: hidden` (would shift the page layout / hide the scrollbar right under the open menu) in favor of wheel/touchmove suppression; the two are different, both-justified techniques, not one superseding the other. This primitive instead fills a real, previously-unaddressed gap: `dialog.rs`/`alert_dialog.rs` lock no scroll at all today (unlike shadcn/Radix's Dialog), and are the intended first adopters via 7.8 migration — no registry component calls `use_scroll_lock` yet. Verified: `cargo fmt --all --check`; `cargo check --locked --workspace`; `cargo check -p adico-primitives` on default/`web`/`native` features and `--target wasm32-unknown-unknown --features web`; `cargo clippy --locked -p adico-cli -p adico-primitives -p adico-registry-core -p adico-test-utils -p adico-xtask --all-targets -- -D warnings` plus explicit `--features web`/`--features native` on `adico-primitives`; `cargo test --locked` for the same five crates and again with `-p adico-primitives --features native` (2 new unit tests on a feature-independent `next_count(previous, acquire)` pure-arithmetic helper — refcount only reaches zero once every holder releases, and releasing below zero saturates rather than underflowing — all passing). Correction during implementation: an initial test called the DOM-touching `acquire_or_release` helper directly, which panicked under `--features web`/`native` with "Must be called from inside a Dioxus runtime" since `document::eval` requires a live Dioxus scope even outside of `use_effect`; fixed by extracting the pure refcount transition into its own `next_count` function with no DOM call, and testing only that — the `document.body.style.overflow` effect itself remains unverified without a browser, as the task anticipated.
- [x] 7.4d Root-cause fix, discovered while scoping 7.4e below: `use_global_escape_listener` and `use_outside_dismiss` share a long-lived, repeatedly-firing `document::eval` listener pattern already documented (`provenance/records/adico-primitives-wave3-overlays.json`, and `tasks.md` 4.5a/4.6 for the same defect class in `virtual_list`) as never registering `addEventListener` in this Dioxus 0.7.9/0.7.10 web runtime, confirmed by real browser + Playwright testing — meaning 7.4a/7.4b's layering logic had, for Escape, nothing working to gate on `web`. Fix the Escape half using the same already-validated pattern `virtual_list`/`dialog`/`popover`/`alert_dialog` independently reinvented per-component: a native Dioxus event handler instead of `document::eval`. Verify: unit-testable only insofar as the refactor compiles and existing doctests/tests keep passing (a hook cannot itself prove a browser fires an event); explicitly record whether the same fix is available for `use_outside_dismiss` or whether that half has no non-`eval` path as a bare hook. Done 2026-08-31: replaced `use_global_escape_listener` with `use_escape_key(on_escape) -> impl FnMut(Event<KeyboardData>) + Clone`, a hook that returns a keydown handler for the caller to wire onto their own root's `onkeydown` (gated by the same `layer::use_layer().is_topmost()` from 7.4a/7.4b), instead of a bare "global" listener — the fix `dialog`/`popover`/`alert_dialog` each already hand-rolled locally (per their own code comments, verified live via `dx serve`) is now centralized as a real primitive. Migrated all three consumers onto it, removing their duplicated local `onkeydown` Escape checks and their now-fully-dead secondary `use_global_escape_listener` calls. `use_global_escape_listener` and its `use_global_keydown_listener` backing function's Escape-specific usage are removed entirely (`use_global_keydown_listener` itself is kept — `toast.rs`'s F6 focus-region shortcut still depends on it, and is consequently likely subject to the same registration defect, a pre-existing gap this task did not fix or newly break, out of scope for 7.4, flagged here rather than silently left implicit). For `use_outside_dismiss`: confirmed there is no non-`eval` path as a bare hook (no native Dioxus document-level pointer event exists to return a handler for), so its doc comment now states the defect plainly instead of implying it works — a real fix needs a composition-level invisible full-viewport backdrop element with a native `onclick`/`onpointerdown` handler (the technique `dialog`'s registry facade already uses for its own outside-dismiss), which is 7.8 migration scope, not a primitive-only fix; `context_menu` and `popover` do not currently have working outside-dismiss on `web`. Also fixed an unrelated, pre-existing `provenance check` failure surfaced while re-running the full validation matrix: `provenance/records/adico-primitives-dialog-select.json` still listed the deleted `select/text_search.rs` (renamed to `typeahead.rs` in task 7.3c) in its `localPaths`; corrected the path. Verified: `cargo fmt --all --check`; `cargo check --locked --workspace`; `cargo check -p adico-primitives` on default/`web`/`native` features and `--target wasm32-unknown-unknown --features web`; `cargo clippy --locked -p adico-cli -p adico-primitives -p adico-registry-core -p adico-test-utils -p adico-xtask --all-targets -- -D warnings` plus explicit `--features web`/`--features native` on `adico-primitives`; `cargo test --locked` for the same five crates and again with `-p adico-primitives --features native` (no new tests — this is a refactor of existing call sites, not new logic — all 91/92 pre-existing doctests and the full unit suite passing unchanged); `cargo run -p adico-xtask -- registry validate` (46 items, unaffected — no registry item's source changed); `cargo run -p adico-xtask -- provenance check` (now passing, 9 records/64 units, after the path fix above); `cargo run -p adico-xtask -- parity` (38/38, unaffected). Not run: `tests/playwright/` — this fix cannot be verified without a live browser, and is exactly the class of change that most needs it; recorded as the standing caveat for all of §7.4.
- [x] 7.4e Fix the focus scope/trap to recognize `[tabindex]` roving-focus items (not just natively focusable elements) in `js/focus-trap.js`'s focusable-element predicate, and add focus guards plus a non-modal scope variant to `use_focus_trap`/`FocusTrapScript`. Verify: this is a JS-predicate and focus-guard change with no meaningful Rust-only unit test — record explicitly that it is unverified without a live browser/Playwright pass, matching the honesty standard set for 7.4b/7.4c's non-Rust-verifiable halves. Done 2026-08-31: rewrote `js/focus-trap.js` (de-minified for maintainability). `focusable()` now treats any element with an explicit `tabindex` attribute as focusable when its `tabIndex >= 0`, before falling back to the fixed native-tag switch — recognizing this crate's roving-focus items (`collection.rs`-managed listbox/menu items toggling `tabindex="0"`/`"-1"`), which the old predicate's tag-only switch always rejected. `FocusTrap` now inserts two visually-hidden, `tabindex="0"` focus-guard sentinels immediately before/after the container, each redirecting focus back inside (`focusNext`/`focusPrevious`) when reached — a backstop beyond the existing keydown-based Tab interception for cases where focus could otherwise escape without a Tab keypress (e.g. a click on non-focusable page content). Added a new `FocusScope` class (record-and-restore only, no treewalker, no guards, no keydown interception) and `window.createFocusScope()`. On the Rust side, `use_focus_trap`'s early `if !is_modal() { return; }` (which meant a non-modal scope did *nothing at all*, not even focus restoration on close) is removed: the effect now always runs, sending `is_modal` as a third value to the eval script, which picks `createFocusTrap`/`createFocusScope` accordingly — a non-modal popover now gets the same open/close focus-restoration courtesy a modal dialog does, just without Tab containment. This is a `document::eval` one-shot request/response call (send 3 values, no long-lived listen loop), the pattern the wave3 provenance record confirms *does* work in this runtime, unlike the pattern fixed in 7.4d — so this change is not suspected of the same defect class, though it remains genuinely unverified without a live browser as the task anticipated. Verified: `cargo fmt --all --check`; `cargo check --locked --workspace`; `cargo check -p adico-primitives` on default/`web`/`native` features and `--target wasm32-unknown-unknown --features web`; `cargo clippy --locked -p adico-cli -p adico-primitives -p adico-registry-core -p adico-test-utils -p adico-xtask --all-targets -- -D warnings` plus explicit `--features web`/`--features native` on `adico-primitives`; `cargo test --locked` for the same five crates and again with `-p adico-primitives --features native` (no new tests — JS-only plus a Rust signature-internal change with no new testable Rust logic — all 91/92 pre-existing doctests and the full unit suite passing unchanged); `cargo run -p adico-xtask -- registry validate/provenance check/parity` (all passing, unaffected). Not run: `tests/playwright/` — this entire task is JS-behavior/live-DOM-focus verification, which this environment cannot exercise; recorded as the standing caveat for all of §7.4.
- [ ] 7.4f (blocked on browser-verification capability) Implement a real DOM portal with `Backdrop`/`Viewport` parts, replacing the current logical VDOM-only relay in `portal.rs`. Deferred: a true DOM-reparenting portal in Dioxus 0.7 fights VDOM reconciliation (the framework owns node placement) and is only meaningfully verifiable with a live browser/Playwright harness, which is not available in this working environment. Do not implement until either a live-browser verification path exists for this crate or the user explicitly accepts an unverified implementation.
- [ ] 7.4 (umbrella) Dismissable-layer, z-order stacking, scroll locking, the Escape root-cause fix, and focus-scope/trap fixes (7.4a–7.4e) are in place; 7.4f (real DOM portal) remains blocked and unimplemented. Verify nested overlay, focus restoration, Escape, and outside-interaction suites, including a case with two or more nested overlays — record explicitly that this crate's overlay behavior is only truly verifiable through `tests/playwright/` against an installed consumer, that `use_outside_dismiss` remains non-functional on `web` pending 7.8's backdrop-composition fix (7.4d), and that this gap is not exercised by 7.4a–7.4e's unit tests alone.
- [x] 7.5a Implement and unit-test the collision-aware placement math itself (side/align/offset/collision-padding resolution, including the side-flip-on-no-room and cross-axis-clamp-not-align-change decisions) plus `Arrow`'s anchor-pointing offset math, as pure functions independent of any DOM. Verify: unit tests covering every side, every align, the flip case, the no-flip-helps case, and the arrow-offset clamp case. Done 2026-08-31: added `packages/adico-primitives/src/positioner.rs`'s `compute_position`/`arrow_offset` (plus private `resolve_side`/`space_for_side`/`place`/`align_cross`/`clamp_cross` helpers) and a `ContentSide::opposite()` method (`lib.rs`) for the flip. 9 unit tests: placement for each side, center/end align, the flip-to-opposite-when-no-room case, the keep-preferred-when-neither-side-helps case, cross-axis clamping instead of overflowing the viewport, and two `arrow_offset` cases (points at anchor center; stays clamped inside the floating box even when the anchor's center falls outside it). Verified: `cargo fmt --all --check`; `cargo check -p adico-primitives` on default/`web`/`native` features and `--target wasm32-unknown-unknown --features web`; `cargo clippy --locked -p adico-cli -p adico-primitives -p adico-registry-core -p adico-test-utils -p adico-xtask --all-targets -- -D warnings` plus explicit `--features web`/`--features native` on `adico-primitives`; `cargo test --locked -p adico-primitives --lib positioner` (9/9 passing) plus the full five-crate suite.
- [x] 7.5b Wire `compute_position`/`arrow_offset` into real `Positioner`/`Arrow` components, measuring the anchor and floating content once via `MountedData::get_client_rect()` (an already-proven one-shot, non-eval measurement, per `move_interaction.rs`) plus one one-shot `document::eval` round trip for the anchor's `getBoundingClientRect()` by id and the viewport size (a single send/receive, not a long-lived listener — the pattern already confirmed to work, unlike the one fixed in 7.4d). Verify SSR safety (renders with no computed offset, since effects don't run under SSR) and that the crate compiles/lints clean on `web`/`native`/`wasm32-unknown-unknown`. Done 2026-08-31: added `Positioner`/`Arrow` components and `PositionerCtx` (anchor rect, resolved position, floating size, shared via context) to `positioner.rs`. `Positioner` measures on `onmounted`, recomputing via the same `compute_position` from 7.5a; `Arrow` reads the shared context to compute its own cross-axis offset via `arrow_offset`. No registry component composes either yet — that migration (replacing each of popover/hover-card/tooltip/select/combobox/dropdown-menu/context-menu/menubar's own placement math) is 7.8 scope, matching this session's established pattern for `layer`/`scroll_lock`/`use_escape_key` (built and proven, consumer migration deferred). Verified: `cargo fmt --all --check`; `cargo check --locked --workspace`; `cargo check -p adico-primitives` on default/`web`/`native` features and `--target wasm32-unknown-unknown --features web` (including the new rustdoc example, which compiles as a doctest); `cargo clippy --locked -p adico-cli -p adico-primitives -p adico-registry-core -p adico-test-utils -p adico-xtask --all-targets -- -D warnings` plus explicit `--features web`/`--features native` on `adico-primitives`; `cargo test --locked` for the same five crates and again with `-p adico-primitives --features native` (92/93 doctests including the new `Positioner`/`Arrow` example, all passing); `cargo run -p adico-xtask -- registry validate/provenance check/parity` (unaffected, all passing). Not run: `tests/playwright/` — the actual measured position/collision behavior in a real browser is unverified, same standing caveat as the rest of §7.
- [ ] 7.5c (blocked on browser-verification capability) Implement resize/intersection/mutation observer bridges so a `Positioner` repositions when the anchor moves, resizes, or scrolls out of view, behind the crate's two real interop tiers — web and native (desktop and mobile share one WebView-based `document::eval` implementation under the `native` feature; see design.md §8a) — with an SSR-safe no-op fallback. Deferred: this needs the same long-lived, repeatedly-firing browser listener pattern `provenance/records/adico-primitives-wave3-overlays.json` documents as non-functional via `document::eval` in this Dioxus 0.7.9/0.7.10 web runtime (the exact defect class task 7.4d fixed for Escape by returning a native-event handler instead — no equivalent native-Dioxus-event substitute exists for arbitrary-element resize/intersection/mutation). Do not implement until either a live-browser verification path exists for this crate (to test whether a fix analogous to 7.4d's is even possible here) or the user explicitly accepts an unverified implementation.
- [ ] 7.5 (umbrella) Placement math and one-shot `Positioner`/`Arrow` components (7.5a–7.5b) are in place; 7.5c (continuous repositioning via observer bridges) remains blocked and unimplemented, and no registry component (popover, hover-card, tooltip, select, combobox, dropdown-menu, context-menu, menubar) has migrated onto `Positioner` yet (7.8 scope). Verify SSR safety and web behavior in target-specific checks, and that every listed component can compose the same `Positioner` without component-specific placement math — this umbrella task is not satisfied by 7.5a–7.5b alone.
- [ ] 7.6 Implement and test a unified `Menu` primitive (`Root`/`Trigger`/`Positioner`/`Popup`/`Item`/`SubmenuRoot`/`SubmenuTrigger`/`CheckboxItem`/`RadioGroup`/`RadioItem`/`Group`/`GroupLabel`/`Separator`) supporting arbitrarily nested submenus, composed on the primitives from 7.3–7.5; verify keyboard navigation (including submenu open/close and typeahead), accessibility, and a nested-submenu browser fixture.
- [ ] 7.7 Implement and test a unified pointer/gesture primitive (press/long-press/drag) consolidating `pointer.rs`, `move_interaction.rs`, and `selectable.rs`'s pointer-drift check; verify `drag_and_drop_list` and `context_menu`'s independently reimplemented long-press can be re-based on it without behavior change.
- [ ] 7.8 Migrate existing components onto the primitives above and record the outcome per component: accordion onto `use_controlled` and the `collapsible` primitive (closing both `accordion.rs` TODOs — controlled version and collapsible-based rewrite); popover, hover-card, tooltip, select, and combobox onto the shared `Positioner`/`Arrow`; context-menu, dropdown-menu, and menubar onto the unified `Menu`; verify each migrated component's `parity.json` `api` dimension evidence is updated and no migrated component regresses a previously-passing dimension.
- [ ] 7.9 Implement and test the Base-UI-parity primitive tier for shadcn/Base UI components adico does not implement yet — field semantics (Field/Fieldset/Form), Number Field, OTP Field, Meter, Autocomplete, Navigation Menu, Preview Card, and Checkbox Group; verify each primitive names the specific component it unblocks and is validated independent of that component's existence.
- [ ] 7.10 Record M6 acceptance: every new or promoted primitive has provenance (if ported), a documented public API, consumer-independent tests, target applicability, and a mapped dependent component, and every migrated existing component (7.8) has updated parity evidence; verify primitive target checks and browser suites pass.

## 8. M7 — Complex current shadcn gaps

- [ ] 8.1 Refresh and group the actual complex missing catalog by M6 primitive dependencies; verify each planned component has an unblocked foundation and a parity checklist.
- [ ] 8.2 Implement the audited command/combobox/navigation family (including Command and Navigation Menu only when still missing) as composition-first registry source; verify keyboard navigation, typeahead, accessibility, installation, and consumer build coverage.
- [ ] 8.3 Implement the audited overlay/gesture family (including Drawer and Carousel only when still missing) on shared layers/drag primitives; verify mobile-responsive, focus, pointer, and platform behavior coverage.
- [ ] 8.4 Implement the audited input/resize family (including Input OTP and Resizable only when still missing) using reusable selection/drag primitives; verify keyboard, paste/selection, accessibility, and interaction coverage.
- [ ] 8.5 Add every completed complex component to `examples/basic-spa`/`examples/basic-ssr`/`tests/installation/*` through the CLI refresh path; verify visual and SSR/hydration validation is recorded for each applicable item.
- [ ] 8.6 Record M7 acceptance: all current complex gaps are complete or explicitly blocked by a named, scheduled primitive/compatibility issue; verify parity output and focused suites pass.

## 9. M8 — Data and application components

- [ ] 9.1 Audit current shadcn Data Table and Chart source/composition requirements against completed adico primitives and registry items; verify the design record identifies replaceable headless data/chart dependencies and avoids an opaque all-purpose widget.
- [ ] 9.2 Implement missing Table-related source composition and the Data Table registry pattern using source-owned Table, Checkbox, Dropdown Menu, Button, Input, and Pagination composition; verify a `tests/installation/*` fixture supports sorting/selection/pagination behavior covered by tests.
- [ ] 9.3 Implement the Chart registry pattern with shadcn-style configurable source and replaceable chart integration; verify examples cover tokens, tooltip/legend states, dark mode, and accessible fallback/documentation.
- [ ] 9.4 Add installation, Cargo conflict, docs, `examples/basic-spa`/`examples/basic-ssr`/`tests/installation/*`, visual, and applicable SSR/platform validation for each application component; verify complete items satisfy all relevant parity dimensions.
- [ ] 9.5 Record M8 acceptance: Data Table and Chart (if present in the refreshed upstream catalog) are source-owned compositional registry offerings with documented dependencies and parity evidence; verify `tests/installation/*` integration checks pass.

## 10. M9 — Newer current shadcn components

- [ ] 10.1 Refresh the upstream catalog and derive the actual newer chat/agent component family after M8, including Attachment, Bubble, Marker, Message, Message Scroller, and Questionnaire only when present/missing; verify each has a dependency and platform assessment.
- [ ] 10.2 Implement generic message/scroll anchoring primitives before dependent chat source items; verify browser tests cover append/prepend anchoring, user scroll intent, observers, SSR safety, and declared desktop behavior.
- [ ] 10.3 Implement the audited chat/agent component batch as source-owned registry components with semantic, accessible composition; verify installation, keyboard/accessibility, responsive, and theme tests for each item.
- [ ] 10.4 Add focused chat/agent `tests/installation/*` coverage, documentation, and parity evidence; verify unimplemented upstream additions remain visibly missing rather than omitted.
- [ ] 10.5 Record M9 acceptance: every current newer component is complete, explicitly in progress, or scheduled with an identified primitive dependency; verify parity reporting and focused integration suites pass.

## 11. M10 — Full parity validation and ongoing synchronization

- [ ] 11.1 Perform an explicit shadcn catalog refresh and reconcile every snapshot item with `parity.json`; verify there are zero untracked entries and zero missing first-party components before declaring full parity.
- [ ] 11.2 Run and remediate the complete required parity checklist for every component: source, API, visual/variants/states, keyboard, accessibility, dark mode, applicable RTL/responsive behavior, examples, CLI, Cargo, docs, web, desktop, and SSR/hydration; verify evidence links are machine-validated.
- [ ] 11.3 Validate official and configured organization registry sources, provenance records, installation fixtures, `examples/basic-spa`/`examples/basic-ssr`/`tests/installation/*` fixtures, browser tests, accessibility tests, visual suite, and platform builds; verify skipped checks are reported with reasons and none are counted as passes.
- [ ] 11.4 Add scheduled/documented maintainer workflows for `cargo xtask registry build`, `cargo xtask registry validate`, company-registry validation, `cargo xtask parity`, upstream Dioxus inventory refresh, shadcn snapshot refresh, and provenance checks; verify normal CI works offline from checked-in snapshots.
- [ ] 11.5 Publish docs/playground parity status and CLI/registry/provenance maintenance documentation, including an organization-registry authoring and default-switching guide; verify examples and docs only advertise completed component dimensions accurately.
- [ ] 11.6 Record M10 acceptance: the refreshed first-party shadcn catalog has zero missing components and every complete record passes its applicable parity dimensions; verify the full release validation matrix is attached to the milestone report.
