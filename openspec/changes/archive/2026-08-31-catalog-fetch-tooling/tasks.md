## 1. Shared schema

- [x] 1.1 Add `packages/adico-xtask/src/catalog/schema.rs` with `CatalogSnapshot`, `CatalogEntry`, `PartEntry`, `PropsSource` (`Explicit(Vec<Prop>)` / `InheritsFrom(String)` / `Unavailable`), `CompositionRef`, all `Serialize`/`Deserialize`; verify with a round-trip unit test (`serde_json::to_string` then `from_str` on a hand-built sample).
- [x] 1.2 Extend `rust_introspect::PropField` with a `default: Option<String>`; verify with a unit test on a `#[derive(Props)]` struct fixture that has both a defaulted and a non-defaulted field.
- [x] 1.3 Add `packages/adico-xtask/src/catalog/mod.rs` with `AxisKind` (`Primitive`/`Component`), `AxisDef { id, kind, fetch }`, and a static `AXES: &[AxisDef]` table registering the four initial axes; verify with a unit test asserting `AXES` has exactly one entry per axis id with the expected kind.
- [x] 1.4 Wire `mod catalog;` into `packages/adico-xtask/src/main.rs` and add the `catalog fetch <axis|all>` CLI arm, dispatching by looking up `id` in `catalog::AXES` (optional `--revision <sha>` for the two Dioxus axes) plus a usage string generated by listing `AXES`; verify `cargo run -p adico-xtask -- catalog fetch` (no axis) prints every registered axis and its kind, and `cargo run -p adico-xtask -- catalog fetch bogus-axis` errors clearly instead of panicking.

## 2. Dioxus axes (dioxus-primitives, dioxus-components)

- [x] 2.1 Add `packages/adico-xtask/src/catalog/dioxus_shared.rs` with the shared tarball-fetch helper: resolve HEAD sha via GitHub commits API (or use `--revision` if passed), download `codeload.github.com/DioxusLabs/<repo>/legacy.tar.gz/<sha>`, extract to a `tempfile::TempDir`; verify with an integration test gated behind a `--ignored`/network feature flag that fetches a small pinned known-good sha and asserts the extracted tree contains an expected file.
- [x] 2.2 Add `packages/adico-xtask/src/catalog/dioxus_primitives.rs`: walk the extracted `primitives/src` tree with `rust_introspect::introspect_file`, build `CatalogSnapshot` (inventory + explicit props with `default`, no composition — primitives are leaves); verify by fetching current HEAD and asserting the resulting entry count is within the same order of magnitude as `BASEUI_COMPONENTS`'s current length.
- [x] 2.3 Add `packages/adico-xtask/src/catalog/dioxus_components.rs`: walk extracted `preview/src/components` (styled) and `primitives/src` (their primitive deps), build `CatalogSnapshot` with composition links from each styled component to the primitive(s) its source imports; verify by asserting at least one known composed component (e.g. Dialog) records a non-empty composition list.
- [x] 2.4 Run `cargo xtask catalog fetch dioxus-primitives` and `cargo xtask catalog fetch dioxus-components`, hand-review, commit `statics/catalogs/dioxus-primitives.json` and `statics/catalogs/dioxus-components.json`.

## 3. Base UI axis

- [x] 3.1 Add the `scraper` dependency to `packages/adico-xtask/Cargo.toml` (dev/build tooling only, per `docs/architecture.md` boundary — confirm it lands in the workspace's xtask-only deps, not a shared crate).
- [x] 3.2 Add `packages/adico-xtask/src/catalog/base_ui.rs`: fetch `https://base-ui.com/react/components` for the component inventory list; verify against the current hand-written `BASEUI_COMPONENTS` name list, printing any drift (this replaces the relocated live-drift check).
- [x] 3.3 Extend `base_ui.rs` to fetch each `https://base-ui.com/react/components/<slug>` page and parse the per-part API reference table (name/type/default/description) into `props_source: explicit`; verify by asserting Dialog's fetched entry has a non-empty `Root` and `Popup` part with at least one prop each.
- [x] 3.4 Make `catalog fetch base-ui` fail with a clear, non-zero-exit error (not a silently empty snapshot) when expected selectors don't match; verify with a unit test feeding a deliberately malformed HTML fixture.
- [x] 3.5 Run `cargo xtask catalog fetch base-ui`, hand-review, commit `statics/catalogs/base-ui.json`.

## 4. shadcn axis

- [x] 4.1 Add `packages/adico-xtask/src/catalog/shadcn.rs`: resolve shadcn's current commit sha via GitHub commits API, fetch the registry index (component list) and each component's `apps/v4/registry/new-york-v4/ui/<name>.tsx` raw source at that pinned sha.
- [x] 4.2 Implement the passthrough/augmentation regex scan described in design.md (`React.ComponentProps<typeof (\w+)(?:\.(\w+))?>` plus trailing `& { ... }` object literal), resolving the wrapped module alias to an axis+component+part via that file's own `import` statements; verify with unit tests against the Dialog fixture from the spike (`DialogContent` → explicit `showCloseButton` + `inherits_from`; `DialogTrigger` → pure `inherits_from`, no explicit props).
- [x] 4.3 Handle the no-match case (`props_source: unavailable`) and verify with a fixture file that has no `React.ComponentProps<typeof ...>` pattern.
- [x] 4.4 Run `cargo xtask catalog fetch shadcn`, hand-review (including checking the actual current Radix-vs-Base-UI mix per design.md's open question), commit `statics/catalogs/shadcn.json`.

## 5. Wire compat tooling to the new catalogs

- [x] 5.1 Update `packages/adico-xtask/src/primitive_compat.rs` to filter `catalog::AXES` by `AxisKind::Primitive` and load `statics/catalogs/<id>.json` for each match (currently `base-ui` and `dioxus-primitives`, without naming them individually in the comparison logic) instead of the hand-written `BASEUI_COMPONENTS` inventory; keep `status` as a hand-maintained table keyed by id, joined against the fetched inventory (default to `not_started` for ids absent from the hand table); verify `cargo test -p adico-xtask` and `cargo run -p adico-xtask -- primitive-compat check` both pass using only committed statics (no network — verify by running with network disabled, e.g. `--offline` cargo env or a sandboxed run).
- [x] 5.2 Remove the live `base-ui.com` GET from `primitive_compat.rs`'s `sync`/`diff` path (moved to `catalog fetch base-ui` in task 3.2); verify by grepping `primitive_compat.rs` for the base-ui.com URL and confirming it only appears in doc comments referencing where the check now lives, or not at all.
- [x] 5.3 Update `packages/adico-xtask/src/component_compat.rs` to filter `catalog::AXES` by `AxisKind::Component` and load `statics/catalogs/<id>.json` for each match (currently `shadcn` and `dioxus-components`) instead of `upstreams/*/catalog.json`; keep `SHADCN_EXCEPTIONS`/`DIOXUS_COMPONENT_EXCEPTIONS` hand-maintained and joined by id; verify `cargo run -p adico-xtask -- component-compat check` passes offline.
- [x] 5.5 Add a test axis (e.g. a fake `component`-kind axis with a stub fetcher) registered only within a `#[cfg(test)]` table, and assert `component_compat`'s kind-filter logic picks it up without any change to `component_compat.rs` beyond the filter — proving the "add a fifth axis without touching compat internals" requirement.
- [x] 5.4 Add regression tests asserting that re-running `sync` after `fetch` does not alter a hand-set `status`/exception entry (per spec's "Hand-maintained judgment data survives fetch and sync" requirement); verify the new test fails on the old code path (temporarily) and passes on the new one.

## 6. Remove the old upstream command and directory

- [x] 6.1 Remove the `upstream dioxus-components ...` CLI arm and its supporting functions (`report_dioxus_components_snapshot`, `refresh_dioxus_components_snapshot`, `inspect_dioxus_components_source`, `dioxus_components_snapshot_path`) from `packages/adico-xtask/src/main.rs`; verify `cargo build -p adico-xtask` succeeds with no dead-code warnings under `-D warnings`.
- [x] 6.2 Delete the `upstreams/` directory; verify `git status` shows only intentional deletions and the new `statics/catalogs/*.json` additions.

## 7. Documentation

- [x] 7.1 Update `UPSTREAMS.md`'s Workflow section and `cargo xtask` command references to describe `catalog fetch <axis>` and `statics/catalogs/`.
- [x] 7.2 Update `docs/adico/m1-primitive-ownership.md`, `docs/adico/m3-migration-queue.md`, `docs/adico/m3-wave5-migration.md`, `docs/adico/m4-parity-audit.md` references from `upstreams/` to `statics/catalogs/`.
- [x] 7.3 Update `openspec/changes/build-adico-component-ecosystem/design.md` and `tasks.md` references from `upstreams/` to `statics/catalogs/` (historical record — update pointers only, not the narrative).
- [x] 7.4 Update the root `CLAUDE.md`/`docs/development.md` command matrix if `cargo run -p adico-xtask -- upstream dioxus-components ...` is listed there, replacing it with `catalog fetch <axis>`.

## 8. Final validation

- [x] 8.1 Run `cargo fmt --all --check`, `cargo check --locked --workspace`, `cargo clippy --locked -p adico-cli -p adico-primitives -p adico-registry-core -p adico-test-utils -p adico-xtask --all-targets -- -D warnings`, `cargo test --locked -p adico-cli -p adico-primitives -p adico-registry-core -p adico-test-utils -p adico-xtask`.
- [x] 8.2 Run `openspec validate build-adico-component-ecosystem --strict` and `openspec validate catalog-fetch-tooling --strict`.
- [x] 8.3 Confirm end-to-end offline story: with network disabled, `cargo run -p adico-xtask -- primitive-compat check` and `cargo run -p adico-xtask -- component-compat check` both succeed using only committed `statics/catalogs/*.json`.
