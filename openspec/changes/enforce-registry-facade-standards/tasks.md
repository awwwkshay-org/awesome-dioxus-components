## 1. Behavior-ownership classification tooling

- [x] 1.1 Extract `component_compat.rs`'s `use adico_primitives::<module>` source-parsing helper (currently inline in `find_primitive_modules`, `packages/adico-xtask/src/component_compat.rs:37-55`) into a shared location both `component_compat.rs` and the new `primitive_usage.rs` call; verify `cargo run -p adico-xtask -- component-compat check` still passes unchanged after the extraction.
- [x] 1.2 Add `packages/adico-xtask/src/primitive_usage.rs` with a record schema (`classification`: `delegated`/`presentational`/`exception`, `primitiveModules: Vec<String>`, optional `reason`/`followUp`) and a loader that reads one file per item from a records directory; verify with a unit test asserting the loader round-trips a hand-built sample record.
- [x] 1.3 Write one record file per current registry item under `statics/primitive_usage/<item-name>.json`, classified per the audit in `design.md` (37 `delegated`, 8 `presentational`, plus `dialog`, `sheet`, and `sidebar` marked `exception` for their documented gaps — backdrop-dismiss workaround and deferred mobile mode respectively — while still listing the primitive modules they do use):
  `accordion`, `alert-dialog`, `aspect-ratio`, `avatar`, `badge`, `button`, `calendar`, `card`, `checkbox`, `collapsible`, `color-picker`, `combobox`, `context-menu`, `date-picker`, `dialog`, `drag-and-drop-list`, `dropdown-menu`, `hover-card`, `input`, `item`, `label`, `menubar`, `mode-toggle`, `pagination`, `popover`, `progress`, `radio-group`, `scroll-area`, `select`, `sheet`, `sidebar`, `skeleton`, `slider`, `switch`, `tabs`, `tag-group`, `textarea`, `theme-builder`, `theme-switcher`, `toast`, `toggle`, `toggle-group`, `toolbar`, `tooltip`, `virtual-list`
  (45 files, one commit); verify each file is valid JSON matching the 1.2 schema and there is exactly one file per non-`cn` item in `registry.json`.
- [x] 1.4 Implement `sync`: for each item, resolve its actual `adico_primitives` module imports (via the extracted helper from 1.1) and whether `registry.json` declares the `adico-primitives` cargo dependency, and rewrite each record's derived fields (`primitiveModules`, detected dependency) while preserving its hand-authored `classification`/`reason`/`followUp` (mirroring `primitive_compat.rs`'s join-by-key pattern, applied per file instead of per table row); verify by running it on the 45 records from 1.3 and confirming no diff (hand-authored records already match detected state).
- [x] 1.5 Implement `check`, failing on: (a) an item with no record file, or a record file with no matching `registry.json` item; (b) a `delegated` item importing an `adico_primitives` module with no matching file under `packages/adico-primitives/src/`; (c) any item where `registry.json`'s declared `adico-primitives` cargo dependency disagrees, in either direction, with whether that item's record lists any `primitiveModules` (applies regardless of `delegated`/`exception` label, since an `exception` item may still genuinely depend on the crate for its non-exceptional behavior); (d) a `presentational` item whose source contains any of `onkeydown`/`onkeyup`/`Key::`/`use_focus_trap`/`use_scroll_lock`/`document::eval`/`onpointerdown`/`onfocusout`/`use_signal`/`use_context_provider`/`use_context`/`use_effect`, or any `adico_primitives` import; (e) a registry file injecting a page-level scroll/overflow style (`html { overflow: hidden; }` or equivalent) while its record lists a primitive module that owns scroll locking; (f) a `presentational` or `exception` record with an empty `reason`, or an `exception` record with an empty `followUp`; verify with one unit test per condition, using small synthetic fixtures, proving it actually fails (not just that today's tree passes).
- [x] 1.6 Add one dedicated regression test per item (`primitive_usage_<item>`, 45 tests total, named after the same 45 items listed in 1.3) that loads `registry/ui/<file>.rs` and that item's own `statics/primitive_usage/<item-name>.json` record and asserts they agree under the 1.5 rules; verify all 45 pass on `main` once 1.3's records are committed, and spot-check that editing one item's source (e.g. temporarily adding an `adico_primitives` import to `button.rs`) fails only that item's own test, not the others.
- [x] 1.7 Implement `diff` (mirroring `primitive_compat.rs diff`'s console-readable drift report between a committed record and a fresh `sync` computation for that item); verify by editing one item's record, running `diff`, and confirming it reports exactly that item.
- [x] 1.8 Wire `primitive-usage sync|check|diff` into `packages/adico-xtask/src/main.rs`'s CLI dispatch and usage string, following the existing `primitive-compat`/`component-compat` arm pattern; verify `cargo run -p adico-xtask -- primitive-usage` (no subcommand) prints usage instead of panicking, and `cargo run -p adico-xtask -- primitive-usage check` runs with no network access reachable (grep the module for `reqwest`/`ureq`/network calls and confirm none exist).
- [x] 1.9 Commit the 45 `statics/primitive_usage/<item-name>.json` records from 1.3 alongside the module.

## 2. Bounded source remediation (R1-R4, merged for overlapping files)

`dialog`, `alert_dialog`, and `sheet` each carry both an R1 defect (redundant
inline scroll-lock style) and an R4 defect (raw `style:` position/z-index).
Fix both in the same file edit, one `registry build` pass, and one CLI
fixture refresh per file, instead of two separate rounds of checksum churn.
Precise per-file findings below are from direct inspection of each file's
current `style` attributes and neighboring `cn(&[...])` class list, not
assumed.

- [x] 2.1 `registry/ui/dialog.rs`: remove the `style { "html {{ overflow: hidden; }}" }` block (line 50, R1 — the wrapped primitive's `use_scroll_lock` already covers this); remove the overlay's `style: "position: fixed; inset: 0; z-index: 50;"` (line 55, R4 — its class list, `cn(&["fixed inset-0 z-50 bg-black/80 ...`, already carries the identical values, so this is a pure deletion); remove the content's `style: "position: fixed; z-index: 51;"` (line 71, R4) and change its class list's `z-50` to `z-[51]` (the inline style's `z-index: 51` is not redundant with the class's `z-50` — it keeps dialog content stacked above the `z-50` overlay, so the value must be preserved, not just dropped). Verify `cargo xtask primitive-usage check` and `cargo xtask styling-usage check` (once 2.x tooling exists) both treat the file as compliant.
- [x] 2.2 `registry/ui/alert_dialog.rs`: apply the identical three fixes as 2.1 (scroll-lock style at line 56; overlay style at line 60 — pure deletion, matches `cn(&["fixed inset-0 z-50 bg-black/80 ...` at both the composed overlay and the inline `cn(&["fixed inset-0 z-50 bg-black/80", ""])` call; content style at line 75 — delete and bump content's `z-50` to `z-[51]`).
- [x] 2.3 `registry/ui/sheet.rs`: remove the scroll-lock style (line 79, R1); remove the overlay's `style: "position: fixed; inset: 0; z-index: 50;"` (line 84, R4 — pure deletion, matches `cn(&["fixed inset-0 z-50 bg-black/80 ...`); remove the content's `style: "position: fixed; z-index: 51;"` (line 103, R4) and bump content's class `z-50` to `z-[51]` (content class is `"fixed z-50 gap-4 bg-background ..."` — `fixed` is already present, only the z-index value needs preserving).
- [x] 2.4 `registry/ui/popover.rs`: remove the `style: "position: fixed; z-index: 50;"` prop (line 45) entirely, with no class addition needed — the wrapped primitive is `Positioner`-backed and already sets its own computed `position: fixed; left: …px; top: …px;` on the same element, and the class list already carries `z-50`.
- [x] 2.5 `registry/ui/hover_card.rs`: remove the `style: "position: absolute; z-index: 50;"` prop (line 50) entirely, no class addition needed — same `Positioner`-backed reasoning as 2.4. Note this one is not merely redundant: the primitive computes `position: fixed`, while the registry facade was asserting `position: absolute` on the same element via `#[props(extends = GlobalAttributes)]` merging, a genuine value conflict this removal resolves rather than just tidies.
- [x] 2.6 `registry/ui/tooltip.rs`: remove the `style: "position: absolute; z-index: 50;"` prop (line 45) entirely, same reasoning and same conflict as 2.5.
- [x] 2.7 `registry/ui/menubar.rs`: remove the `style: "position: absolute;"` prop from `MenubarContent` (line 50) and add the `absolute` utility class to its class list (`"z-50 min-w-[12rem] overflow-hidden rounded-md border bg-popover p-1 ..."` currently has no position utility at all) — unlike the three `Positioner`-backed files above, `packages/adico-primitives/src/menubar.rs` computes no position itself (verified: no `Positioner`/`position:` reference in that file), so `MenubarContent` still needs `position: absolute` from somewhere, and it must come from the Tailwind class now that the raw `style` prop is gone.
- [x] 2.8 `registry/ui/mode_toggle.rs`: remove the redundant `let mut open = use_signal(|| false)` open-state and its `open`/`on_open_change` wiring into `DropdownMenu` (R2), leaving the primitive's own uncontrolled state (`use_controlled`, `packages/adico-primitives/src/menu.rs:104`) in charge; verify the component still opens/closes correctly in a manual `dx serve` pass from `apps/playground`.
- [x] 2.9 Correct `packages/adico-primitives/src/scroll_lock.rs`'s module doc (R3), which currently claims modal scroll lock is "a real gap today, since neither currently locks scroll at all" — no longer true once `dialog`/`alert_dialog` call it; verify by re-reading the corrected comment against `dialog.rs:245`/`alert_dialog.rs:258`.
- [x] 2.10 Add or extend Playwright coverage in `tests/playwright` asserting: scroll lock engages while a dialog/sheet/alert-dialog is open and releases on close, including a nested-dialog case where only the outer dialog's close restores page scroll (the refcount behavior the removed inline style could not provide, R1); and Popover, HoverCard, and Tooltip content still opens anchored to its trigger at the expected side/alignment after the `style`-prop removal in 2.4-2.6 (the concrete regression risk named in `design.md`'s Risks section, R4). Verify `cd tests/playwright && npm test` passes.
- [x] 2.11 Run `cargo xtask registry build` to regenerate `registry.json` checksums and `registry/generated/*` for all 7 edited files (`dialog`, `alert_dialog`, `sheet`, `popover`, `hover_card`, `tooltip`, `menubar`) plus `mode_toggle`; verify `cargo xtask registry validate` passes.
- [x] 2.12 Refresh every installed copy of the 8 touched components through the CLI, never by hand: `apps/playground`, `examples/basic-spa`, `examples/basic-ssr` (via `scripts/refresh-basic-example.sh`'s pattern extended to cover `dialog`, `sheet`, `alert_dialog`, `mode_toggle`, `popover`, `hover_card`, `tooltip`, `menubar`), and `tests/installation/{dialog-consumer,wave1-consumer,wave2-risk-consumer,theme-consumer}`; verify each refreshed project still builds (`cargo check` for the relevant package).

## 3. Styling classification tooling

Authored against the tree as fixed by section 2, so every record reflects
final state — no item needs a temporary "known defect" exception, and every
`tailwindOnly: false`/`tokenCompliant: false` entry records a genuine,
permanent judgment call.

- [x] 3.1 Add `packages/adico-xtask/src/styling_usage.rs` with a record schema (`tailwindOnly: bool`, `styleException: Vec<{ description: String, reason: String }>`, `tokenCompliant: bool`, `colorException: Vec<{ value: String, reason: String }>`, `inspiredBy: Vec<"shadcn" | "dioxus-components">`, `inspirationNote: String`) and a loader that reads one file per item from `statics/styling_usage/`, mirroring `primitive_usage.rs`'s loader shape (per Decisions in `design.md`: a sibling module, not a third field on `primitive_usage.rs`'s records); verify with a unit test asserting the loader round-trips a hand-built sample record.
- [x] 3.2 Write one record file per current registry item under `statics/styling_usage/<item-name>.json` (the same 45 items listed in 1.3), classified per the styling/token audit in `design.md`:
  - `tailwindOnly: true`, no `styleException`, for every item with no runtime-computed style value — including all 7 files fixed in section 2, now that their raw `style:` props are gone.
  - `tailwindOnly: false` with a `styleException` entry for `progress.rs` (computed indicator width), `sidebar.rs` (`--sidebar-width`/`--sidebar-width-icon` custom properties), and `theme-builder` (generated preview styles) — genuinely dynamic values that cannot be static Tailwind classes.
  - `tokenCompliant: true`, no `colorException`, for items with no non-token color use.
  - `tokenCompliant: false` with a `colorException` entry for the five identified legitimate non-token uses: `button.rs`'s destructive `text-white`, `dialog`/`alert-dialog`/`sheet`'s `bg-black/80` overlay (each citing the matching shadcn `new-york-v4` source), `color-picker`'s `border-white` thumb, `theme-switcher`'s preset swatches, and `tag-group`'s `black/10`/`white/10` hover tint.
  - `inspiredBy`/`inspirationNote` left as `[]`/empty for now — populated per item in section 6.
  Verify each file is valid JSON matching the 3.1 schema and there is exactly one file per non-`cn` item in `registry.json`.
- [x] 3.3 Implement `sync`: for each item, detect `style { ... }` blocks and `style:` attributes and raw hex/rgb/default-palette color classes in `registry/ui/<file>.rs`, and rewrite each record's derived detection fields while preserving its hand-authored `styleException`/`colorException`/`inspiredBy`/`inspirationNote` entries (mirroring `primitive_usage.rs sync`'s preserve-judgment-fields pattern); verify by running it on the 45 records from 3.2 and confirming no diff.
- [x] 3.4 Implement `check`, failing on: (a) an item with no record file, or a record file with no matching `registry.json` item; (b) a `tailwindOnly: true` item whose source contains a `style { ... }` block or a `style:` attribute with no matching `styleException` entry; (c) a `tokenCompliant: true` item whose source contains a raw hex/rgb color or a Tailwind default-palette color class with no matching `colorException` entry; (d) any `styleException` or `colorException` entry with an empty `reason`; verify with one unit test per condition, using small synthetic fixtures, proving it actually fails (not just that today's tree passes).
- [x] 3.5 Add one dedicated regression test per item (`styling_usage_<item>`, 45 tests total) that loads `registry/ui/<file>.rs` and that item's own `statics/styling_usage/<item-name>.json` record and asserts they agree under the 3.4 rules; verify all 45 pass once 3.2's records are committed, and spot-check that editing one item's source (e.g. temporarily adding a raw `style:` attribute to `button.rs`) fails only that item's own test.
- [x] 3.6 Implement `diff` (same shape as `primitive_usage.rs diff`); verify by editing one item's record, running `diff`, and confirming it reports exactly that item.
- [x] 3.7 Wire `styling-usage sync|check|diff` into `packages/adico-xtask/src/main.rs`'s CLI dispatch and usage string, following the same arm pattern as `primitive-usage`; verify `cargo run -p adico-xtask -- styling-usage` (no subcommand) prints usage instead of panicking, and confirm no network calls in the module.
- [x] 3.8 Commit the 45 `statics/styling_usage/<item-name>.json` records from 3.2 alongside the module.

## 4. CI gate

- [x] 4.1 Add `cargo run -p adico-xtask -- primitive-usage check` and `cargo run -p adico-xtask -- styling-usage check` as steps in `.github/workflows/ci.yml`, alongside the existing `fmt`/`check`/`clippy`/`test` steps; verify the workflow file still parses (`gh workflow view ci.yml` or equivalent) and both steps run locally with the same commands.
- [x] 4.2 Prove the `primitive-usage` gate fires: temporarily remove `adico-primitives` from one `delegated` item's `cargoDependencies` in `registry.json`, run `cargo xtask primitive-usage check` locally and confirm it fails with a clear message identifying that item, then revert the temporary edit; verify by capturing the failure output before reverting.
- [x] 4.3 Prove the `styling-usage` gate fires: temporarily reintroduce a raw `style:` attribute with no matching exception into one `tailwindOnly` item, run `cargo xtask styling-usage check` locally and confirm it fails with a clear message identifying that item, then revert the temporary edit; verify by capturing the failure output before reverting.

## 5. Spec delta

- [x] 5.1 Write `specs/adico-registry/spec.md` (`## ADDED Requirements`, no H1): one requirement that every registry UI/component item declares a behavior-ownership classification, one that the classification is mechanically verified offline and gated in CI, one that every registry item declares a styling classification (Tailwind-only usage and semantic-token usage, each with bounded reasoned exceptions), and one that the styling classification is mechanically verified offline and gated in CI; scope the scroll-style scenario narrowly to the page-level-overflow-injection check and the raw-CSS scenario narrowly to what the checker actually detects (a `style`/`style:` construct with no matching exception), not general duplication or styling-quality detection; verify `openspec validate enforce-registry-facade-standards --strict` passes.

## 6. Dual-reference styling audit

- [ ] 6.1 For each of the 45 items listed in 1.3, read shadcn's pinned `new-york-v4` source (where a shadcn equivalent exists) and dioxus-components' pinned preview source (where a dioxus-components equivalent exists) — network access required, one-time — and compare classes and semantic-token usage against adico's current `registry/ui/<file>.rs`; record the outcome in that item's `statics/styling_usage/<item-name>.json` `inspiredBy` (which of `shadcn`/`dioxus-components` it was checked against) and `inspirationNote` (a one-line summary of what, if anything, was corrected, or that no divergence was found). If network access to either upstream source is unavailable in the executing environment, leave `inspiredBy`/`inspirationNote` empty and record the audit as skipped-with-reason in task 8.4 rather than fabricating a note.
- [ ] 6.2 Fix any genuine divergence found in 6.1 (a missing token, a missing state class, an idiom either upstream handles better) directly in the affected `registry/ui/*.rs` file; regenerate checksums via `cargo xtask registry build` and refresh installed copies through the CLI for any file changed this way.
- [ ] 6.3 For deliberate adico-specific departures with no upstream equivalent (for example, Badge's `Verified` variant, which neither shadcn nor dioxus-components has), record them in `inspirationNote` as intentional additions, not gaps; verify no such item is left with an empty `inspirationNote` unless 6.1's network exception applies.
- [ ] 6.4 Commit the updated `statics/styling_usage/<item-name>.json` records from 6.1-6.3 together with any source fixes from 6.2.

## 7. Documentation

- [x] 7.1 Add `primitive-usage sync|check|diff` and `styling-usage sync|check|diff` to the `adico-xtask` command list in `CLAUDE.md` and `docs/development.md`.
- [x] 7.2 Add `primitive-usage check` and `styling-usage check` to the validation matrix in `docs/validation.md`.
- [x] 7.3 Add a short note to `docs/architecture.md` recording why `registry/` remains at the repository root instead of moving under `packages/` (summarize the Decisions section of `design.md`), and a short note on the Tailwind-only/semantic-token styling rule alongside the existing per-project Tailwind pipeline description.

## 8. Final validation

- [ ] 8.1 Run `cargo fmt --all --check`, `cargo check --locked --workspace`, `cargo clippy --locked -p adico-cli -p adico-primitives -p adico-registry-core -p adico-test-utils -p adico-xtask --all-targets -- -D warnings`, `cargo test --locked -p adico-cli -p adico-primitives -p adico-registry-core -p adico-test-utils -p adico-xtask`.
- [ ] 8.2 Run `cargo xtask registry validate`, `cargo xtask primitive-usage check`, `cargo xtask styling-usage check`, `cargo xtask component-compat check`, `cargo xtask primitive-compat check`.
- [ ] 8.3 Run `openspec validate enforce-registry-facade-standards --strict` and `openspec validate build-adico-component-ecosystem --strict`.
- [ ] 8.4 Report which surfaces were exercised (Playwright, `dx serve`) versus skipped (desktop, SSR, wasm32, the network-dependent dual-reference audit) for the R1/R2/R4 behavior changes and section 6, per `docs/validation.md`'s honesty requirement — do not claim a skipped surface passed.
