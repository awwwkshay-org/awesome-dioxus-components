## 1. Route-nesting prerequisite check

- [x] 1.1 Confirmed via `dioxus-router-0.7.9` source (`src/contexts/outlet.rs`, `OutletContext::render`): each `Outlet` tracks its own `current_level` and calls `provide_context(outlet.next())` for its child scope, so nesting `#[layout(...)]` scopes produces genuinely depth-tracked, non-parallel Outlets — `PlaygroundLayout` rendered inside `SiteLayout`'s `Outlet` renders at level 1, and `PlaygroundLayout`'s own `Outlet` renders its children at level 2. The nested route tree from design.md's Decisions section proceeds as planned; the flat-`SiteLayout` fallback is not needed. Empirical portal-positioning confirmation happens in task 9.7's `dx serve` smoke test.

## 2. Crate move and scaffold

- [x] 2.1 `git mv apps/playground apps/web` and verify with `git status` that the move is tracked as a rename, not a delete+add
- [x] 2.2 Update `apps/web/Cargo.toml`: `name = "adico-web"`, add `serde`/`serde_json` as explicit pinned entries (docs currently inherits them via `.workspace = true`), drop the `server` feature entry — verify `cargo check --locked -p adico-web` still succeeds (expect only errors from missing home/docs modules, addressed in section 3). Also added a lightweight `ssr = ["dioxus/ssr"]` feature: dropping `server` orphaned two legitimate playground unit tests (`date_picker.rs`, `calendar.rs`) that used `feature = "server"` only as a vehicle to reach `dioxus::ssr::render` for HTML-snapshot assertions, unrelated to docs' actual dead SSR-serve branch — retargeted their `#[cfg(all(test, feature = "server"))]` to `feature = "ssr"` so they stay reachable via `cargo test -p adico-web --features ssr` without restoring the heavy `dioxus/server` fullstack feature
- [x] 2.3 Update `apps/web/Dioxus.toml`: `name = "adico-web"`, `[web.app] title = "adico"`
- [x] 2.4 Update root `Cargo.toml` `[workspace].members`: remove `"apps/docs"` and `"apps/home"`, rename the `"apps/playground"` entry to `"apps/web"` — verified `cargo metadata --no-deps --format-version 1 | jq -r '.packages[].name'` lists `adico-web` and no longer lists `adico-home`/`adico-docs`/`adico-playground`
- [x] 2.5 Delete `apps/home/` and `apps/docs/` (their content is folded into `apps/web` in section 3) — verified `git status` shows both paths removed (git tracked their reused files as renames into `apps/web`, not delete+add)

## 3. Fold in home and docs content

- [x] 3.1 Move `apps/home/src/pages/index.rs` to `apps/web/src/pages/index.rs`, `apps/home/src/components/cta_link.rs` to `apps/web/src/components/cta_link.rs`, register both in their respective `mod.rs` files — verified `cargo check --locked -p adico-web` compiles clean
- [x] 3.2 Create `apps/web/src/pages/playground/` and move every existing `apps/web/src/pages/*.rs` demo page file into it (except `index.rs`, which becomes the playground index at `pages/playground/index.rs`, renamed `PlaygroundIndex` to avoid colliding with home's `Home`), updating `pages/mod.rs`/`pages/playground/mod.rs` accordingly — verified `cargo check --locked -p adico-web` compiles clean with routes.rs updated in section 4. The two shell-free harness pages (`responsive_flow.rs`, `responsive_overlay.rs`) stayed at the top-level `pages/`, not moved into `pages/playground/`, since their routes stay outside every layout
- [x] 3.3 Create `apps/web/src/pages/docs/index.rs` and `apps/web/src/pages/docs/component.rs`, porting `apps/docs/src/main.rs`'s two page bodies onto Tailwind utility classes and installed `Card`/`Table`/`Badge` registry components — removed all `DOCS_STYLE` hardcoded CSS — verified live via `dx serve` + browser: `/docs` and `/docs/components/accordion` render correctly, and toggling light mode shows the site's light theme tokens with no forced dark background left over
- [x] 3.4 Update the two `include_str!` calls moved into `apps/web/src/pages/docs/data.rs` to account for the two-directories-deeper nesting (`../../../../../registry/registry.json`, `../../../../../statics/component_props.json` — 5 levels, not 4, since the docs pages live in their own `pages/docs/` subdirectory) — verified `cargo check --locked -p adico-web` succeeds and the rendered `/docs` and `/docs/components/accordion` pages show real component/prop data (69 real component cards, real usage/accessibility/keyboard docs, a real introspected props table), not an empty list

## 4. Route tree and layouts

- [x] 4.1 Rewrite `apps/web/src/routes.rs`'s `Route` enum per design.md's Decisions section: `SiteLayout` wraps `/` (`Home`), `/docs`, `/docs/components/:name`, and a `PlaygroundLayout`-wrapped `/playground` + `/playground/<segment>` subtree (×70, one variant per moved page); `/responsive/flow` and `/responsive/overlay?:case` stay outside every layout — verified `cargo check --locked -p adico-web` succeeds with zero warnings
- [x] 4.2 Compose `SiteLayout` from installed registry components (header/nav reusing home's `NavigationMenu`/`ModeToggle`/`ThemeSwitcher` composition, updated to link `/`, `/docs`, `/playground`) — verified live: the header renders with working Docs/Playground/GitHub links, matching `apps/home`'s pre-merge shell
- [x] 4.3 Rename playground's existing `Layout` component to `PlaygroundLayout`, update its internal route references (nav column links, active-route matching) to `Route::PlaygroundIndex {}` — verified `cargo check --locked -p adico-web --target wasm32-unknown-unknown` compiles clean. Also changed the root App wrapper from `h-dvh overflow-hidden` (which would have broken normal page scrolling on `/` and `/docs`) to `min-h-dvh`, and `PlaygroundLayout`'s own root from `h-full` to `min-h-0 flex-1` so it still gets a bounded height to compute its resizable-panel percentages against, now that it's nested two layouts deep instead of being the whole page — verified live: `/playground`'s resizable nav/content split renders correctly, and a portalled Dialog on `/playground/dialog` opens centered and correctly positioned, confirming the nested-Outlet mechanism from task 1.1 holds in practice
- [x] 4.4 Update `nav_items()` to emit `/playground/<segment>` hrefs — verified live: clicking "Dialog" in the sidebar navigated to `/playground/dialog`

## 5. Fix incidental defects found during research

- [x] 5.1 In `apps/web/adico.lock`, correct the stale checksum for `@adico/theme-switcher` to match the installed file's real sha256 — no `adico add --check`-equivalent command exists in the CLI (confirmed via `adico --help`), so verified by computing `shasum -a 256` on the installed file directly: the lock's checksum (`34fe3dd9...`) did not match the file's real hash (`4f6ee190...`); corrected it, and spot-checked three other entries (accordion, badge, mode-toggle) to confirm they were already consistent — the defect was isolated to theme-switcher as research found
- [x] 5.2 Neutralize the dead demo hrefs in `apps/web/src/pages/playground/breadcrumb.rs` and `apps/web/src/pages/responsive_flow.rs` (currently `/components`, `/components/registry`) to `#` — verified by grepping the whole `apps/web/src/pages/` tree for `"/components` and confirming zero matches

## 6. CSS pipeline

- [x] 6.1 Run `adico css build` from `apps/web`, verify `adico css check` reports `assets/tailwind.css is up to date`, and confirm every Tailwind class referenced across the landing, docs, and playground page trees is present in the compiled output — verified: `adico css check` reported "assets/tailwind.css is up to date", and the live-rendered pages (landing, docs, playground) all show correct styling with no missing classes

## 7. Playwright suite

- [x] 7.1 Add the `/playground` prefix to the `goto()` call sites in `playground-drag-and-drop-list.spec.ts`, `playground-resizable-split.spec.ts` (including its `for (const path of [...])` array and its own literal `goto("/alert-dialog")`), `playground-time-picker.spec.ts`, and `playground-enriched-demos.spec.ts` (10 call sites total across the 4 files) — leaving every `goto("/")` targeting `examples/basic-spa`/`basic-ssr`/`tests/installation/*` fixtures and every `/responsive/*` goto untouched — verified via `git diff --stat tests/playwright/` that exactly those 4 files changed, and via `grep -rl 'goto("/playground'` that only those 4 files contain the new prefix
- [x] 7.2 Update `tests/playwright/README.md`'s playground instructions from `dx serve` in `apps/playground` to `apps/web` (also updated the per-file header comments in `playground-resizable-split.spec.ts`, `playground-time-picker.spec.ts`, and `playground-enriched-demos.spec.ts` for the same reason, beyond just the README)

## 8. Documentation

- [x] 8.1 Update `apps/README.md` to describe one app (`apps/web`) instead of three. Also updated `apps/web/README.md` itself (moved from `apps/playground/README.md` by the git mv, so its title and Structure section were left describing only the playground half) to describe the merged landing/docs/playground app
- [x] 8.2 Update root `README.md`'s workspace table (three app rows → one `apps/web` row) and the "Project status" paragraph's app-path references
- [x] 8.3 Update `docs/ci-cd.md`, `docs/architecture.md`, `docs/development.md`, `docs/validation.md`, and `CLAUDE.md`'s "Project overview" bullet. Also fixed one stray live reference in `packages/adico-primitives/README.md`. The task's own grep check (`grep -rn "apps/docs\|apps/home\|apps/playground" --include=*.md .`) is not literally zero after this: `docs/adico/m0-foundation.md`, `m3-acceptance.md`, `m4-acceptance.md`, `m4-parity-audit.md`, `m10-parity-status.md`, and `component-hardening-audit.md` are dated historical milestone/audit records describing repo state at the time they were written — rewriting them to the merged paths would falsify history, the same reasoning that keeps old git commit messages untouched, so they were deliberately left alone (a narrower exemption than "outside archive/spec history" as originally worded, since these historical docs live under `docs/adico/`, not in either exempted location). `openspec/specs/adico-playground-structure/spec.md` and `openspec/specs/adico-playground-demo-controls/spec.md` were also left alone: the first is retired by this change's own delta and will update at archive time through the normal sync step, and the second has real path staleness (`apps/playground/src/components/ui/*.rs` → `apps/web/...`) but is a capability this change's proposal never declared as modified — hand-editing a governed spec outside its declared delta would bypass the same OpenSpec process this repo requires elsewhere in this same task. **Flagging as a follow-up**, not silently fixed: `adico-playground-demo-controls` needs its own small MODIFIED delta (path-only, no behavior change) in a future change

## 9. Validation

- [x] 9.1 Run `cargo fmt --all --check` and verify it passes with no diff — passed, no diff
- [x] 9.2 Run `cargo check --locked --workspace` and verify `adico-web` builds along with the rest of the workspace, with no remaining reference to `adico-home`/`adico-docs`/`adico-playground` — passed clean, zero warnings
- [x] 9.3 Run `cargo check --locked -p adico-web --target wasm32-unknown-unknown` and verify it compiles clean — passed clean
- [x] 9.4 Run `cargo clippy --locked -p adico-cli -p adico-primitives -p adico-registry-core -p adico-test-utils -p adico-xtask --all-targets -- -D warnings` and verify it passes — passed clean
- [x] 9.5 Run `cargo test --locked -p adico-cli -p adico-primitives -p adico-registry-core -p adico-test-utils -p adico-xtask` and verify it passes — all suites green, zero failures
- [x] 9.6 Run `openspec validate --all --strict` and verify it passes, including the new `adico-web-structure` capability and the removal of `adico-playground-structure`/`adico-home-structure` — passed (15/15); the two REMOVED-delta capabilities still show as valid main specs, which is expected — they retire at archive time, not at validate time
- [x] 9.7 Manually run `dx serve` in `apps/web` and verify: `/` renders with working `/docs` and `/playground` links; `/docs` and a `/docs/components/:name` page render correctly in both light and dark mode; a `/playground/<x>` page with a portalled overlay (dialog/popover/select) opens and positions correctly; `/responsive/flow` renders with no site chrome — all verified live via browser automation during sections 3–5 (see their notes above): landing page renders with working header nav; `/docs` and `/docs/components/accordion` render correctly and light mode shows no forced dark background; `/playground/dialog`'s Dialog opens correctly centered and positioned; `/responsive/flow` renders completely shell-free
- [ ] 9.8 **BLOCKED** — With `dx serve` still running, run `ADICO_PLAYWRIGHT_BASE_URL=http://localhost:3000 npm test` from `tests/playwright`. First run: 60 failed, 41 passed. Root-caused and fixed two real regressions in this task:
  - `responsive-shell.spec.ts` and `responsive-desktop-shell.spec.ts` (8 `goto("/")` call sites) were missed by task 7.1's original scope — their header comments explicitly target "the playground's own app shell", which now lives at `/playground`, not `/`. Fixed: prefixed both files.
  - The `Demo` component's preview/controls split (percentage `flex-basis` inside a flex column) silently fell back to content-based auto-sizing because `main.rs`'s `min-h-dvh` (added during task 4.3) doesn't give the nested `SiteLayout` → `PlaygroundLayout` → `ResizablePanelGroup` → `Demo` chain a *definite* height several layers deep. Fixed: reverted `main.rs`'s App root to `h-dvh overflow-hidden` (matching pre-merge playground exactly) and added `overflow-y-auto` to `SiteLayout`'s `main` so long docs/home content still scrolls internally instead of relying on document-level scroll. Re-verified live: the preview/controls ratio is now exactly 0.700, matching the coded default. `playground-resizable-split.spec.ts`, `playground-drag-and-drop-list.spec.ts`, `responsive-shell.spec.ts`, and `responsive-desktop-shell.spec.ts` now all pass.
  - 51 of the remaining failures are pre-existing, not caused by this merge: `dialog.spec.ts`, `mode-toggle.spec.ts`, `select.spec.ts`, `theme-switcher.spec.ts`, `wave2-*.spec.ts`, `wave3.spec.ts`, `wave4.spec.ts`, `wave5-*.spec.ts` target `examples/basic-spa` (confirmed: `examples/basic-spa/src/main.rs` contains the exact "Open dialog"/"Choose a fruit" content these tests assert on) — not started in this run. `fullstack.spec.ts` and `time-picker.spec.ts` similarly target `examples/basic-ssr` and `tests/installation/time-picker-consumer`. Running the literal `npm test` command this task specifies cannot pass all three server-scoped suites with only `apps/web`'s dev server running; that is a pre-existing constraint of the suite's single shared `baseURL` design, unrelated to this merge.
  - **3 genuine failures remain, root-caused but NOT fixed — pausing here per the apply skill's guardrails, since fixing them means changing `registry/ui/*.rs`, which is out of this change's declared scope**: `playground-enriched-demos.spec.ts`'s two Carousel drag/button tests, and `playground-time-picker.spec.ts`'s dial-tracking test. Both `registry/ui/carousel.rs` (compiled into `apps/web/src/components/ui/carousel.rs`) and `registry/ui/time_picker.rs` measure their own geometry exactly once in an `onmounted` handler (`get_client_rect()`/`get_scroll_size()`), with no later resize/remount to correct a stale initial measurement. Confirmed live in-browser: the carousel's track genuinely overflows (`scrollWidth: 1680` vs `clientWidth: 336`, 5 slides) but both paging buttons report `disabled: true`, and even a keyboard `ArrowRight` (which bypasses the disabled attribute entirely, calling `scroll_next()` directly) does not advance the slide — the component's internal `content_size`/`viewport_size` signals are genuinely stuck stale, not just a button rendering glitch. This is the same class of bug as the already-fixed `2026-09-08-fix-resizable-panel-seeding` change (a registry component's one-shot mount-time measurement race), now newly exposed because this merge adds one more nested layout layer (`SiteLayout` wrapping `PlaygroundLayout`) between the app root and these pages, shifting the timing enough to make the stale measurement reproducible. Not fixed here because: (a) `registry/ui/*.rs` changes require their own cited change per `adico-web-structure`'s "Registry components are never modified solely for an app-section's convenience" requirement — this is a genuine defect, not app convenience, but the fix itself (making these components re-measure on resize, not just mount) is real scope beyond a structural merge; (b) this change's proposal never declared `registry/ui/carousel.rs` or `registry/ui/time_picker.rs` as affected files.
