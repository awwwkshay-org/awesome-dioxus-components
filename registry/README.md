# Adico registry source

This directory is the authored source distribution layer for adico. It is not a
styled Rust component crate. The M2 registry vertical slice introduces the
versioned manifest and source metadata beneath this boundary.
