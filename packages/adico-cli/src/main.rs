//! The `adico` command-line interface.
//!
//! Command parsing and installation behavior are introduced by the M2 registry
//! vertical slice. This crate exists now so its public executable boundary is
//! explicit in the workspace.

fn main() {
    eprintln!("adico is not available yet; the registry vertical slice is in progress.");
}
