# Deployment

The former Todo API/UI container deployment has been retired. `adico` does not
yet define a containerized production application or release artifact.

Future delivery work will publish the CLI, owned primitives, registry artifacts,
and documentation application only after the M2/M3 installation pipeline and
release requirements are implemented. Until then, use the local development
checks in [`development.md`](development.md).
