# Maintained adico applications

`web/` is the maintained Dioxus application for adico's public site (the
landing page, docs, and component playground, all served from one router —
see its own `README.md`) and must not replace consumer-style installation
fixtures under `examples/` and `tests/installation/`.
